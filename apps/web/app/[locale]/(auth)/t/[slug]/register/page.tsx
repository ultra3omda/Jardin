/**
 * V1.6 — Path-based pre-auth tenant register route.
 * Delegates to the V1.5 register UI; layout already injected the brand.
 */
import RegisterPage from '@/app/(auth)/register/page';
export default RegisterPage;
