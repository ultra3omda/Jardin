/**
 * V1.6 — Convenience re-export of the shared color utilities.
 * Lets web code import from a local module without leaking the
 * @ecole-saas/shared import path into every file.
 */
export { hexToHslTriplet, isHexColor } from '@ecole-saas/shared';
