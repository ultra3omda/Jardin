# TEACHER 3.2 — Pédagogie mobile (évaluations) Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline).

**Goal:** Aligner les écrans mobile évaluations enseignant sur les états Vague 1 (Skeleton/ErrorState).

**Scope (réduit après mapping) :** seuls `pedagogy/evaluations/index.tsx` et
`pedagogy/evaluations/[id].tsx` sont legacy (ActivityIndicator + texte d'erreur). `evaluations/new.tsx`
= déjà moderne (FormField/Picker/Button + erreurs inline). `pedagogy/index.tsx` = PARTIAL acceptable
(états locaux fonctionnels) → laissé. **Aucun backend.**

**Branche / worktree :** `feat/teacher-3.2-pedagogie-mobile` (fresh origin/main, install gelée).

---

## Task 1 — `evaluations/index.tsx`
- Import `ErrorState`, `Skeleton` ; retirer `ActivityIndicator`.
- Loading → `Skeleton` ×4. Error (Pressable+Text) → `ErrorState`+`refetch`. `EmptyState` déjà partagé (OK).
Commit: `feat(mobile): evaluations list states (Skeleton/ErrorState)`.

## Task 2 — `evaluations/[id].tsx` (saisie notes)
- Import `ErrorState`, `EmptyState`, `Skeleton` ; retirer `ActivityIndicator`.
- Loading → `Skeleton` ×5. `detail.isError` → `ErrorState`(`detail.refetch`). No-students → `EmptyState`.
- Saisie/`mutation`/seed inchangés.
Commit: `feat(mobile): grade entry states (Skeleton/ErrorState/EmptyState)`.

## Task 3 — Vérif + PR
type-check mobile (+ web cohérence) ; push ; PR base main ; CI verte → merge ; STOP avant 3.3.

---

## Self-Review
- Spec §4 (3.2) évals mobile ✓ ; `new` déjà moderne, `pedagogy/index` PARTIAL acceptable (noté).
- Backend : 0. Mutations/seed/`isClosed` intacts.
- Types : `Skeleton`/`ErrorState`/`EmptyState` exportés (V2/V3) ; `refetch` via useQuery. Install gelée.
- Présentationnel → couverture via type-check (pas de helper pur ici).
