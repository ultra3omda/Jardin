# V1.7-A Mobile Scaffold — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Scaffolder `apps/mobile` (Expo SDK 51) avec login + dashboard placeholder + brand runtime, ET ajouter le middleware web host-resolver dormant gated par `ENABLE_SUBDOMAIN_RESOLVER`.

**Architecture:** Monorepo pnpm/Turbo — `apps/mobile` est un package Expo SDK 51 avec Expo Router (file-based), NativeWind v4 pour le styling, Zustand pour l'auth store en mémoire + SecureStore pour le refresh token, et le hook `useTenantBrand` qui interroge `/api/public/tenant-brand/:slug`. Le flag `EXPO_PUBLIC_PERSONA` (parent|teacher|admin|dev) sélectionne l'UX cible (fusionné en V12 en 3 builds EAS). Le middleware Next.js est étendu avec un bloc dormant gated par `ENABLE_SUBDOMAIN_RESOLVER=true` (inactif par défaut).

**Tech Stack:** Expo SDK 51 · React Native 0.74.5 · Expo Router ~3.5.0 · NativeWind v4 · Zustand ^5 · TanStack Query ^5 · expo-secure-store ~13.0.0 · i18next · expo-localization · TypeScript strict · pnpm workspaces · Turborepo

**Spec source:** `docs/superpowers/specs/2026-05-24-v1.7-mobile-scaffold-design.md`

**Roadmap:** Décision D21 — `docs/roadmap.md`

**Merge policy (CLAUDE.md locked 2026-05-22):** CI verte → merge automatique immédiat (`gh pr merge --merge`), pas d'attente OK utilisateur.

---

## ⚠️ Checkpoints obligatoires (s'ARRÊTER et demander)

| # | Quand | Pourquoi |
|---|---|---|
| CP-1 | Avant d'ajouter scripts dans `package.json` **racine** (Phase B Task 6) | CLAUDE.md : checkpoint obligatoire sur package.json racine |
| CP-2 | Avant d'installer des deps natives (`expo-secure-store`, etc.) (Phase B Task 6) | Deps natives = changement de lockfile significatif |
| CP-3 | Avant de modifier `turbo.json` (Phase B Task 6) | Impact sur tous les pipelines CI |
| CP-4 | Avant de toucher `.github/workflows/` (Phase G Task 5) | CLAUDE.md : checkpoint obligatoire |

---

## 🗂️ Decomposition des fichiers

### Nouveaux fichiers créés

```
apps/mobile/
  package.json                          — deps Expo SDK 51 lockées
  app.json                              — config Expo (slug, bundleIdentifier, persona)
  babel.config.js                       — preset expo + nativewind
  metro.config.js                       — monorepo watchFolders + withNativeWind
  tailwind.config.js                    — NativeWind thème + content paths
  tsconfig.json                         — extend @ecole-saas/typescript-config/react-native
  .env.example                          — EXPO_PUBLIC_API_URL, EXPO_PUBLIC_PERSONA
  README.md                             — quick start 5 min pour débutant mobile
  app/
    _layout.tsx                         — root layout : QueryClient + i18n + boot router
    index.tsx                           — boot router (redirige selon auth/tenantSlug)
    (onboarding)/
      _layout.tsx                       — layout onboarding (fond neutre)
      school-code.tsx                   — écran saisie code établissement
    (auth)/
      _layout.tsx                       — layout auth (fond brand)
      login.tsx                         — écran login email+password
    (app)/
      _layout.tsx                       — layout app authentifié (tab nav)
      dashboard.tsx                     — placeholder dashboard avec PERSONA
  lib/
    api/
      client.ts                         — fetch wrapper (baseURL, token, refresh)
      auth.ts                           — login(), refresh(), logout()
      tenant.ts                         — getTenantBrand(slug)
    auth/
      store.ts                          — Zustand auth store (mémoire uniquement)
      secure-storage.ts                 — wrapper expo-secure-store (refresh token + tenantSlug)
    tenant/
      store.ts                          — Zustand tenant store (brand en mémoire)
      use-tenant-brand.ts               — hook fetch+cache brand (TanStack Query)
    i18n/
      index.ts                          — init i18next + expo-localization
      locales/fr.json                   — 16 clés UI

apps/web/
  lib/tenant/
    extract-tenant-slug.ts              — helper extractTenantSlugFromHost + RESERVED_SLUGS

packages/typescript-config/
  react-native.json                     — tsconfig pour projets Expo

docs/
  adr/0004-mobile-expo-scaffold.md      — ADR décision stack mobile
  superpowers/runbooks/v1.7-b-dns-activation.md  — runbook 5 min ops DNS+Vercel
```

### Fichiers modifiés

```
apps/web/middleware.ts                  — ajouter bloc dormant host-resolver
turbo.json                             — ajouter pipeline mobile (dev, build)
package.json (racine)                   — ajouter scripts mobile (avec CP-1)
```

---

## 📱 TUTORIEL MOBILE — À lire avant la Phase A

> **TU ES DÉBUTANT EN MOBILE ? LIS CE TUTORIEL EN ENTIER AVANT DE COMMENCER.**
> Tu n'as pas besoin d'un simulateur ou d'un émulateur pour tester l'application.
> **Expo Go** sur ton smartphone est suffisant et c'est le chemin le plus simple.

### Étape 1 — Installer Expo Go sur ton téléphone

**iOS (iPhone) :**
1. Ouvre l'**App Store**
2. Recherche **"Expo Go"**
3. Installe l'application gratuite de **Expo** (logo blanc fond noir)

**Android :**
1. Ouvre le **Google Play Store**
2. Recherche **"Expo Go"**
3. Installe l'application gratuite

### Étape 2 — Lancer le serveur de développement

Depuis ton PC, dans le terminal du projet :

```bash
pnpm --filter=@klasso/mobile start
```

Tu verras dans le terminal :

```
Metro waiting on exp://192.168.x.x:8081
› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web

┌──────────────────────────────────────────────────┐
│                                                  │
│  ██████╗ ██████╗     QR code ici                 │
│  ██╔══██╗██╔══██╗                                │
│  ██████╔╝██████╔╝                                │
│  ██╔══██╗██╔══██╗                                │
│  ██████╔╝██║  ██║                                │
│  ╚═════╝ ╚═╝  ╚═╝                                │
│                                                  │
│  exp://192.168.1.x:8081                          │
└──────────────────────────────────────────────────┘
```

### Étape 3 — Scanner le QR code avec ton téléphone

**iPhone :** Ouvre l'application **Appareil photo** → pointe vers le QR code → appuie sur la notification qui apparaît → Expo Go s'ouvre automatiquement.

**Android :** Ouvre **Expo Go** → appuie sur "Scan QR code" → scanne le QR.

⚠️ **Ton téléphone et ton PC doivent être sur le même réseau Wi-Fi.**

### Étape 4 — Live reload (rechargement automatique)

Chaque fois que tu modifies un fichier `.tsx` et sauvegardes, l'application se recharge automatiquement sur ton téléphone en **~1-2 secondes**. Pas besoin de rescanner le QR code.

### Étape 5 — Menu développeur (Debug menu)

Pour accéder aux outils de debug :
- **Secoue ton téléphone** physiquement (shake gesture)
- Un menu apparaît avec : Reload, Debug JS Remotely, Inspector, Performance Monitor

### Alternative — Simulateur iOS (Mac uniquement)

Si tu as un Mac avec Xcode installé :
```bash
pnpm --filter=@klasso/mobile start
# Puis appuie sur 'i' dans le terminal
```

### Alternative — Émulateur Android (lourd, optionnel)

Nécessite Android Studio (~8GB). Non recommandé pour débuter.
Si tu veux quand même l'installer : https://docs.expo.dev/workflow/android-studio-emulator/

---

## 🗺️ STRUCTURE DE L'ÉCRAN (ce que tu verras sur ton téléphone)

```
Phase D — Écran school-code :
┌────────────────────────────┐
│        Klasso              │
│                            │
│   Bienvenue !              │
│   Code de votre école      │
│                            │
│  ┌──────────────────────┐  │
│  │  mon-ecole           │  │
│  └──────────────────────┘  │
│  [   Continuer   ]         │
│                            │
└────────────────────────────┘

Phase E — Écran login :
┌────────────────────────────┐
│   [LOGO ÉCOLE]             │
│   Collège Victor Hugo      │
│                            │
│  ┌──────────────────────┐  │
│  │  email@ecole.fr      │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │  ••••••••            │  │
│  └──────────────────────┘  │
│  [  Se connecter (brand)  ]│
│  Changer d'école           │
└────────────────────────────┘

Phase F — Dashboard :
┌────────────────────────────┐
│  [LOGO]  Collège V. Hugo   │
│                            │
│  Bonjour, Jean Dupont      │
│  Rôle : Direction          │
│  Persona : admin           │
│                            │
│  [  Se déconnecter  ]      │
└────────────────────────────┘
```

---

## Phase A — Middleware host-resolver dormant + helper + tests (≈2h)

### Task A1 — Créer le tsconfig react-native

**Files:**
- Create: `packages/typescript-config/react-native.json`
- Modify: `packages/typescript-config/package.json`

- [ ] **Step A1.1 : Créer `packages/typescript-config/react-native.json`**

```json
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "React Native / Expo",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020"],
    "allowJs": true,
    "jsx": "react-native",
    "strict": true,
    "noEmit": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "baseUrl": "."
  }
}
```

- [ ] **Step A1.2 : Ajouter l'export dans `packages/typescript-config/package.json`**

Lire le fichier actuel, puis ajouter `"react-native.json"` dans le champ `exports` :

```json
{
  "name": "@ecole-saas/typescript-config",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./base.json": "./base.json",
    "./nextjs.json": "./nextjs.json",
    "./nestjs.json": "./nestjs.json",
    "./react-native.json": "./react-native.json"
  }
}
```

- [ ] **Step A1.3 : Commit**

```bash
git add packages/typescript-config/react-native.json packages/typescript-config/package.json
git commit -m "chore(config): add react-native tsconfig for Expo SDK 51"
```

---

### Task A2 — Helper `extractTenantSlugFromHost` + tests unitaires

**Files:**
- Create: `apps/web/lib/tenant/extract-tenant-slug.ts`
- Create: `apps/web/lib/tenant/__tests__/extract-tenant-slug.test.ts`

- [ ] **Step A2.1 : Écrire les tests (TDD — RED)**

Créer `apps/web/lib/tenant/__tests__/extract-tenant-slug.test.ts` :

```typescript
import { describe, it, expect } from 'vitest';
import {
  extractTenantSlugFromHost,
  RESERVED_SLUGS,
} from '../extract-tenant-slug';

describe('extractTenantSlugFromHost', () => {
  it('extrait le slug pour <slug>.klasso.tn', () => {
    expect(extractTenantSlugFromHost('mon-ecole.klasso.tn', 'klasso.tn')).toBe('mon-ecole');
  });

  it('extrait le slug pour <slug>.klasso.fr', () => {
    expect(extractTenantSlugFromHost('victor-hugo.klasso.fr', 'klasso.fr')).toBe('victor-hugo');
  });

  it('retourne null pour le domaine racine', () => {
    expect(extractTenantSlugFromHost('klasso.tn', 'klasso.tn')).toBeNull();
  });

  it('retourne null pour www', () => {
    expect(extractTenantSlugFromHost('www.klasso.tn', 'klasso.tn')).toBeNull();
  });

  it('retourne null pour app (reservé)', () => {
    expect(extractTenantSlugFromHost('app.klasso.tn', 'klasso.tn')).toBeNull();
  });

  it('retourne null pour localhost', () => {
    expect(extractTenantSlugFromHost('localhost:3000', 'klasso.tn')).toBeNull();
  });

  it('retourne null pour un host qui ne correspond pas au baseDomain', () => {
    expect(extractTenantSlugFromHost('evil.other.com', 'klasso.tn')).toBeNull();
  });

  it('retourne null si le slug contient des caractères invalides', () => {
    expect(extractTenantSlugFromHost('MAJUSCULE.klasso.tn', 'klasso.tn')).toBeNull();
  });

  it('accepte un slug avec tirets valides', () => {
    expect(extractTenantSlugFromHost('ecole-victor-hugo.klasso.tn', 'klasso.tn')).toBe('ecole-victor-hugo');
  });
});
```

- [ ] **Step A2.2 : Lancer les tests — vérifier qu'ils échouent (RED)**

```bash
pnpm --filter=@ecole-saas/web test -- --run lib/tenant/__tests__/extract-tenant-slug
```

Attendu : erreur "Cannot find module '../extract-tenant-slug'"

- [ ] **Step A2.3 : Implémenter `extract-tenant-slug.ts`**

Créer `apps/web/lib/tenant/extract-tenant-slug.ts` :

```typescript
/**
 * V1.7-A — Helper: extract tenant slug from host header.
 *
 * Utilisé par le middleware Next.js (dormant tant que
 * ENABLE_SUBDOMAIN_RESOLVER !== 'true').
 *
 * Pattern attendu : <slug>.<baseDomain>
 * Ex : mon-ecole.klasso.tn → 'mon-ecole'
 */

/** Slugs réservés — jamais mappés à un tenant */
export const RESERVED_SLUGS = new Set([
  'www',
  'app',
  'api',
  'admin',
  'assets',
  'docs',
  'status',
  'mail',
  'support',
  'dashboard',
]);

/** Regex pour valider un slug tenant : lowercase, chiffres, tirets */
const SLUG_REGEX = /^[a-z0-9][a-z0-9-]{0,61}[a-z0-9]$|^[a-z0-9]$/;

/**
 * Extrait le slug tenant depuis le host HTTP.
 *
 * @param host       - valeur de l'en-tête `Host` (ex: "mon-ecole.klasso.tn")
 * @param baseDomain - domaine racine sans sous-domaine (ex: "klasso.tn")
 * @returns slug tenant ou null si non applicable
 */
export function extractTenantSlugFromHost(
  host: string,
  baseDomain: string,
): string | null {
  // Retirer le port si présent
  const hostname = host.split(':')[0];

  // Vérifier que le host se termine par .<baseDomain>
  const suffix = `.${baseDomain}`;
  if (!hostname.endsWith(suffix)) return null;

  const slug = hostname.slice(0, hostname.length - suffix.length);

  // Slug vide = domaine racine
  if (!slug) return null;

  // Slug réservé
  if (RESERVED_SLUGS.has(slug)) return null;

  // Format invalide
  if (!SLUG_REGEX.test(slug)) return null;

  return slug;
}
```

- [ ] **Step A2.4 : Lancer les tests — vérifier GREEN**

```bash
pnpm --filter=@ecole-saas/web test -- --run lib/tenant/__tests__/extract-tenant-slug
```

Attendu : `9 passed`

- [ ] **Step A2.5 : Commit**

```bash
git add apps/web/lib/tenant/extract-tenant-slug.ts apps/web/lib/tenant/__tests__/extract-tenant-slug.test.ts
git commit -m "feat(web/tenant): add extractTenantSlugFromHost helper + 9 unit tests"
```

---

### Task A3 — Middleware web : ajouter le bloc host-resolver dormant

**Files:**
- Modify: `apps/web/middleware.ts`

- [ ] **Step A3.1 : Lire `apps/web/middleware.ts` (état actuel)**

Le fichier gère actuellement les redirections auth (login/dashboard). On va ajouter un bloc **avant** la logique auth, gated par `ENABLE_SUBDOMAIN_RESOLVER`.

- [ ] **Step A3.2 : Modifier `apps/web/middleware.ts`**

Remplacer le contenu entier par :

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { REFRESH_COOKIE_NAME } from '@/lib/auth/cookies';
import { extractTenantSlugFromHost } from '@/lib/tenant/extract-tenant-slug';

const PROTECTED_PREFIXES = ['/dashboard'];
const AUTH_PREFIXES = ['/login', '/register'];

/**
 * V1.7-A — Bloc host-resolver dormant.
 *
 * Actif uniquement si ENABLE_SUBDOMAIN_RESOLVER=true.
 * En V1.7-A (avant livraison klasso.tn) : inactif.
 * En V1.7-B (après DNS OVH + Vercel domain) : activer via env Vercel.
 *
 * Quand actif, redirige <slug>.klasso.tn/* → /t/<slug>/*
 * afin de rester compatible avec le routage path-based V1.6.
 */
const BASE_DOMAIN = process.env.NEXT_PUBLIC_BASE_DOMAIN ?? 'klasso.tn';
const SUBDOMAIN_RESOLVER_ENABLED =
  process.env.ENABLE_SUBDOMAIN_RESOLVER === 'true';

export function middleware(request: NextRequest): NextResponse {
  const host = request.headers.get('host') ?? '';
  const path = request.nextUrl.pathname;

  // ── Bloc host-resolver (dormant en V1.7-A) ──────────────────────────
  if (SUBDOMAIN_RESOLVER_ENABLED) {
    const slug = extractTenantSlugFromHost(host, BASE_DOMAIN);
    if (slug) {
      // Éviter la boucle : /t/<slug> est déjà le path target
      if (!path.startsWith(`/t/${slug}`)) {
        const url = request.nextUrl.clone();
        url.pathname = `/t/${slug}${path}`;
        return NextResponse.rewrite(url);
      }
    }
  }
  // ────────────────────────────────────────────────────────────────────

  // ── Auth redirects (V1.5 inchangé) ──────────────────────────────────
  const hasRefreshCookie = !!request.cookies.get(REFRESH_COOKIE_NAME);

  if (
    PROTECTED_PREFIXES.some((p) => path === p || path.startsWith(`${p}/`)) &&
    !hasRefreshCookie
  ) {
    const url = request.nextUrl.clone();
    url.pathname = '/login';
    url.searchParams.set('next', path);
    return NextResponse.redirect(url);
  }

  if (AUTH_PREFIXES.some((p) => path === p) && hasRefreshCookie) {
    const url = request.nextUrl.clone();
    url.pathname = '/dashboard';
    return NextResponse.redirect(url);
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/dashboard/:path*', '/login', '/register', '/((?!_next|api|favicon).*)'],
};
```

- [ ] **Step A3.3 : Type-check**

```bash
pnpm --filter=@ecole-saas/web type-check
```

Attendu : 0 errors

- [ ] **Step A3.4 : Commit**

```bash
git add apps/web/middleware.ts
git commit -m "feat(web/middleware): add dormant subdomain host-resolver (ENABLE_SUBDOMAIN_RESOLVER gate)"
```

---

### Task A4 — Ouvrir PR Phase A + CI + merge

- [ ] **Step A4.1 : Pousser la branche et ouvrir la PR**

```bash
git push -u origin chore/v1.7-naming-klasso
gh pr create --title "feat(v1.7-A): middleware host-resolver dormant + extractTenantSlugFromHost" \
  --body "$(cat <<'EOF'
## Phase A — V1.7-A

### Changements
- `packages/typescript-config/react-native.json` — nouveau tsconfig Expo SDK 51
- `apps/web/lib/tenant/extract-tenant-slug.ts` — helper + 9 tests unitaires
- `apps/web/middleware.ts` — bloc dormant host-resolver (ENABLE_SUBDOMAIN_RESOLVER gate)

### Tests
- 9 unit tests `extractTenantSlugFromHost` passent
- Type-check web : 0 errors

### Activation V1.7-B
Ajouter `ENABLE_SUBDOMAIN_RESOLVER=true` dans Vercel env après livraison DNS OVH klasso.tn.

Spec: docs/superpowers/specs/2026-05-24-v1.7-mobile-scaffold-design.md
EOF
)"
```

- [ ] **Step A4.2 : Attendre CI verte puis merger**

```bash
gh pr checks --watch
gh pr merge --merge
```

---

## Phase B — Scaffold `apps/mobile` (≈2h)

### Task B1 — `packages/typescript-config/react-native.json` déjà créé (Task A1) ✓

### Task B2 — `apps/mobile/package.json`

**Files:**
- Create: `apps/mobile/package.json`

- [ ] **Step B2.1 : Créer `apps/mobile/package.json`**

```json
{
  "name": "@klasso/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "dependencies": {
    "@ecole-saas/shared": "workspace:*",
    "@tanstack/react-query": "^5.56.2",
    "expo": "~51.0.0",
    "expo-constants": "~16.0.0",
    "expo-font": "~12.0.0",
    "expo-linking": "~6.3.0",
    "expo-localization": "~15.0.0",
    "expo-router": "~3.5.0",
    "expo-secure-store": "~13.0.0",
    "expo-splash-screen": "~0.27.0",
    "expo-status-bar": "~1.12.0",
    "i18next": "^23.15.1",
    "nativewind": "^4.1.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-i18next": "^15.0.2",
    "react-native": "0.74.5",
    "react-native-safe-area-context": "4.10.5",
    "react-native-screens": "3.31.1",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@ecole-saas/typescript-config": "workspace:*",
    "@types/react": "~18.2.79",
    "@types/react-native": "~0.74.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.3.0"
  }
}
```

- [ ] **Step B2.2 : Commit**

```bash
git add apps/mobile/package.json
git commit -m "chore(mobile): add package.json with Expo SDK 51 dependencies"
```

---

### Task B3 — Config files Expo

**Files:**
- Create: `apps/mobile/app.json`
- Create: `apps/mobile/tsconfig.json`
- Create: `apps/mobile/.env.example`

- [ ] **Step B3.1 : Créer `apps/mobile/app.json`**

```json
{
  "expo": {
    "name": "Klasso",
    "slug": "klasso",
    "version": "1.7.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "splash": {
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "ios": {
      "bundleIdentifier": "tn.klasso.app",
      "supportsTablet": false
    },
    "android": {
      "adaptiveIcon": {
        "backgroundColor": "#ffffff"
      },
      "package": "tn.klasso.app"
    },
    "web": {
      "bundler": "metro"
    },
    "plugins": [
      "expo-router",
      "expo-secure-store",
      [
        "expo-splash-screen",
        {
          "backgroundColor": "#ffffff"
        }
      ]
    ],
    "experiments": {
      "typedRoutes": true
    },
    "scheme": "klasso"
  }
}
```

- [ ] **Step B3.2 : Créer `apps/mobile/tsconfig.json`**

```json
{
  "extends": "@ecole-saas/typescript-config/react-native.json",
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.d.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step B3.3 : Créer `apps/mobile/.env.example`**

```bash
# URL de l'API Klasso (sans trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:4000

# Persona active : parent | teacher | admin | dev
# En V1.7-A : utiliser 'dev' pour voir tous les écrans
EXPO_PUBLIC_PERSONA=dev
```

- [ ] **Step B3.4 : Commit**

```bash
git add apps/mobile/app.json apps/mobile/tsconfig.json apps/mobile/.env.example
git commit -m "chore(mobile): add app.json config, tsconfig, env.example"
```

---

### Task B4 — Metro config (monorepo + NativeWind)

**Files:**
- Create: `apps/mobile/metro.config.js`
- Create: `apps/mobile/babel.config.js`
- Create: `apps/mobile/tailwind.config.js`

- [ ] **Step B4.1 : Créer `apps/mobile/metro.config.js`**

```javascript
// Metro config pour monorepo pnpm + NativeWind v4
// Voir: https://docs.expo.dev/guides/monorepos/
const { getDefaultConfig } = require('expo/metro-config');
const { withNativeWind } = require('nativewind/metro');
const path = require('path');

const projectRoot = __dirname;
const workspaceRoot = path.resolve(projectRoot, '../..');

const config = getDefaultConfig(projectRoot);

// 1. Watcher monorepo — surveille packages/* depuis la racine
config.watchFolders = [workspaceRoot];

// 2. Resolver monorepo — cherche les node_modules en remontant
config.resolver.nodeModulesPaths = [
  path.resolve(projectRoot, 'node_modules'),
  path.resolve(workspaceRoot, 'node_modules'),
];

// 3. Désactiver la recherche hiérarchique pour éviter les doublons React
config.resolver.disableHierarchicalLookup = true;

// 4. Transpiler @ecole-saas/shared (paquet workspace TS)
config.resolver.sourceExts = [...(config.resolver.sourceExts ?? []), 'mjs'];

module.exports = withNativeWind(config, {
  input: './global.css',
});
```

- [ ] **Step B4.2 : Créer `apps/mobile/babel.config.js`**

```javascript
module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
    plugins: [
      // NativeWind v4 — doit être en premier
      'nativewind/babel',
      // Expo Router typed routes
      require.resolve('expo-router/babel'),
    ],
  };
};
```

- [ ] **Step B4.3 : Créer `apps/mobile/tailwind.config.js`**

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './lib/**/*.{ts,tsx}',
  ],
  presets: [require('nativewind/preset')],
  theme: {
    extend: {
      colors: {
        // Brand colors injectées via CSS vars (runtime tenant)
        brand: {
          primary: 'rgb(var(--brand-primary) / <alpha-value>)',
          secondary: 'rgb(var(--brand-secondary) / <alpha-value>)',
        },
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step B4.4 : Créer `apps/mobile/global.css`** (requis par NativeWind v4)

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step B4.5 : Créer placeholder assets**

```bash
mkdir -p apps/mobile/assets
# Créer un icon.png placeholder (1024x1024 blanc) — Expo en a besoin au démarrage
# Pour un test local, copier n'importe quelle image PNG et la renommer icon.png
# En V12 (EAS), on mettra les vraies icônes par persona
```

Note: Expo ne bloque pas le démarrage si `assets/icon.png` est absent en dev, mais si le build plante, copier n'importe quelle image PNG 1024×1024.

- [ ] **Step B4.6 : Commit**

```bash
git add apps/mobile/metro.config.js apps/mobile/babel.config.js apps/mobile/tailwind.config.js apps/mobile/global.css
git commit -m "chore(mobile): Metro monorepo config + NativeWind v4 + Babel preset"
```

---

### Task B5 — Dossier `app/` squelette Expo Router

**Files:**
- Create: `apps/mobile/app/_layout.tsx` (root layout)
- Create: `apps/mobile/app/index.tsx` (boot router)
- Create: `apps/mobile/app/(onboarding)/_layout.tsx`
- Create: `apps/mobile/app/(auth)/_layout.tsx`
- Create: `apps/mobile/app/(app)/_layout.tsx`

- [ ] **Step B5.1 : Créer `apps/mobile/app/_layout.tsx`** (root — minimal pour Phase B)

```tsx
import '../global.css';
import { Stack } from 'expo-router';
import { StatusBar } from 'expo-status-bar';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: { retry: 2, staleTime: 5 * 60 * 1000 },
  },
});

export default function RootLayout() {
  return (
    <QueryClientProvider client={queryClient}>
      <Stack screenOptions={{ headerShown: false }} />
      <StatusBar style="auto" />
    </QueryClientProvider>
  );
}
```

- [ ] **Step B5.2 : Créer `apps/mobile/app/index.tsx`** (boot router minimal)

```tsx
import { Redirect } from 'expo-router';

/**
 * Boot router — Phase B (minimal).
 * Phase C l'enrichira avec la logique auth + tenantSlug.
 */
export default function Index() {
  // En Phase B : toujours envoyer vers onboarding pour test visuel
  return <Redirect href="/(onboarding)/school-code" />;
}
```

- [ ] **Step B5.3 : Créer les layouts de groupe**

`apps/mobile/app/(onboarding)/_layout.tsx` :

```tsx
import { Stack } from 'expo-router';

export default function OnboardingLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
```

`apps/mobile/app/(auth)/_layout.tsx` :

```tsx
import { Stack } from 'expo-router';

export default function AuthLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
```

`apps/mobile/app/(app)/_layout.tsx` :

```tsx
import { Stack } from 'expo-router';

export default function AppLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
```

- [ ] **Step B5.4 : Commit**

```bash
git add apps/mobile/app/
git commit -m "chore(mobile): Expo Router app directory skeleton (layouts + boot index)"
```

---

### Task B6 — ⚠️ CP-1 + CP-2 + CP-3 : Turbo + pnpm root scripts + install deps

> **CHECKPOINT CP-1 : Modification `package.json` racine**
> **CHECKPOINT CP-2 : Installation deps natives (expo-secure-store, etc.)**
> **CHECKPOINT CP-3 : Modification `turbo.json`**
>
> ⛔ S'ARRÊTER ici et demander confirmation utilisateur avant de continuer.

Une fois l'accord obtenu :

- [ ] **Step B6.1 : Modifier `turbo.json`** — ajouter pipeline mobile

Lire `turbo.json` actuel, puis ajouter dans `tasks` :

```json
"@klasso/mobile#start": {
  "cache": false,
  "persistent": true
},
"@klasso/mobile#type-check": {
  "dependsOn": ["^build", "^type-check"],
  "outputs": []
},
"@klasso/mobile#lint": {
  "dependsOn": [],
  "outputs": []
}
```

- [ ] **Step B6.2 : Modifier `package.json` racine** — ajouter scripts mobile

Ajouter dans `scripts` :

```json
"dev:mobile": "turbo run start --filter=@klasso/mobile",
"type-check:mobile": "turbo run type-check --filter=@klasso/mobile"
```

- [ ] **Step B6.3 : Installer les dépendances**

```bash
pnpm install
```

Attendu : lockfile mis à jour, pas d'erreur.

- [ ] **Step B6.4 : Vérifier que type-check passe**

```bash
pnpm --filter=@klasso/mobile type-check
```

Attendu : 0 errors (les fichiers squelettes sont suffisamment typés)

- [ ] **Step B6.5 : Commit**

```bash
git add turbo.json package.json pnpm-lock.yaml
git commit -m "chore(monorepo): add @klasso/mobile to turbo pipeline + root scripts"
```

- [ ] **Step B6.6 : Ouvrir PR Phase B + attendre CI + merger**

```bash
gh pr create --title "feat(v1.7-A Phase B): scaffold apps/mobile Expo SDK 51" \
  --body "Scaffold complet : package.json, app.json, Metro monorepo, NativeWind v4, Expo Router skeleton, turbo pipeline."
gh pr checks --watch && gh pr merge --merge
```

---

## Phase C — Theme provider + brand runtime + i18n (≈2h)

### Task C1 — SecureStorage wrapper

**Files:**
- Create: `apps/mobile/lib/auth/secure-storage.ts`

- [ ] **Step C1.1 : Créer `apps/mobile/lib/auth/secure-storage.ts`**

```typescript
import * as SecureStore from 'expo-secure-store';

/** Clés de stockage sécurisé */
export const STORAGE_KEYS = {
  REFRESH_TOKEN: 'klasso_refresh_token',
  TENANT_SLUG: 'klasso_tenant_slug',
} as const;

export async function saveRefreshToken(token: string): Promise<void> {
  await SecureStore.setItemAsync(STORAGE_KEYS.REFRESH_TOKEN, token);
}

export async function getRefreshToken(): Promise<string | null> {
  return SecureStore.getItemAsync(STORAGE_KEYS.REFRESH_TOKEN);
}

export async function deleteRefreshToken(): Promise<void> {
  await SecureStore.deleteItemAsync(STORAGE_KEYS.REFRESH_TOKEN);
}

export async function saveTenantSlug(slug: string): Promise<void> {
  await SecureStore.setItemAsync(STORAGE_KEYS.TENANT_SLUG, slug);
}

export async function getSavedTenantSlug(): Promise<string | null> {
  return SecureStore.getItemAsync(STORAGE_KEYS.TENANT_SLUG);
}

export async function deleteTenantSlug(): Promise<void> {
  await SecureStore.deleteItemAsync(STORAGE_KEYS.TENANT_SLUG);
}
```

- [ ] **Step C1.2 : Commit**

```bash
git add apps/mobile/lib/auth/secure-storage.ts
git commit -m "feat(mobile/auth): SecureStore wrapper (refresh token + tenant slug)"
```

---

### Task C2 — API client mobile

**Files:**
- Create: `apps/mobile/lib/api/client.ts`
- Create: `apps/mobile/lib/api/auth.ts`
- Create: `apps/mobile/lib/api/tenant.ts`

- [ ] **Step C2.1 : Créer `apps/mobile/lib/api/client.ts`**

```typescript
import { getRefreshToken, saveRefreshToken } from '@/lib/auth/secure-storage';

const BASE_URL = process.env.EXPO_PUBLIC_API_URL ?? 'http://localhost:4000';

export class ApiError extends Error {
  constructor(
    public readonly status: number,
    message: string,
  ) {
    super(message);
    this.name = 'ApiError';
  }
}

/** Token access en mémoire (non persisté) */
let _accessToken: string | null = null;

export function setAccessToken(token: string | null): void {
  _accessToken = token;
}

export function getAccessToken(): string | null {
  return _accessToken;
}

async function fetchApi<T>(
  path: string,
  options: RequestInit = {},
  withAuth = true,
): Promise<T> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(options.headers as Record<string, string>),
  };

  if (withAuth && _accessToken) {
    headers['Authorization'] = `Bearer ${_accessToken}`;
  }

  const response = await fetch(`${BASE_URL}${path}`, {
    ...options,
    headers,
  });

  if (!response.ok) {
    const body = await response.json().catch(() => ({}));
    throw new ApiError(response.status, body.message ?? response.statusText);
  }

  return response.json() as Promise<T>;
}

export { fetchApi };
```

- [ ] **Step C2.2 : Créer `apps/mobile/lib/api/auth.ts`**

```typescript
import { fetchApi, setAccessToken } from './client';
import {
  saveRefreshToken,
  getRefreshToken,
  deleteRefreshToken,
  deleteTenantSlug,
} from '@/lib/auth/secure-storage';
import type { AuthSessionResponse } from '@ecole-saas/shared';

export interface LoginInput {
  email: string;
  password: string;
  tenantSlug?: string;
}

export async function login(input: LoginInput): Promise<AuthSessionResponse> {
  const session = await fetchApi<AuthSessionResponse>('/api/auth/login', {
    method: 'POST',
    body: JSON.stringify(input),
  }, false);

  setAccessToken(session.accessToken);
  if (session.refreshToken) {
    await saveRefreshToken(session.refreshToken);
  }
  return session;
}

export async function refreshSession(): Promise<AuthSessionResponse | null> {
  const refreshToken = await getRefreshToken();
  if (!refreshToken) return null;

  try {
    const session = await fetchApi<AuthSessionResponse>('/api/auth/refresh', {
      method: 'POST',
      body: JSON.stringify({ refreshToken }),
    }, false);

    setAccessToken(session.accessToken);
    if (session.refreshToken) {
      await saveRefreshToken(session.refreshToken);
    }
    return session;
  } catch {
    return null;
  }
}

export async function logout(): Promise<void> {
  try {
    await fetchApi('/api/auth/logout', { method: 'POST' });
  } catch {
    // Ignorer les erreurs réseau au logout
  }
  setAccessToken(null);
  await deleteRefreshToken();
  await deleteTenantSlug();
}
```

- [ ] **Step C2.3 : Créer `apps/mobile/lib/api/tenant.ts`**

```typescript
import { fetchApi } from './client';
import type { TenantBrand } from '@ecole-saas/shared';

export interface PublicTenantBrandResponse {
  slug: string;
  name: string;
  brand: TenantBrand;
}

export async function getTenantBrand(slug: string): Promise<PublicTenantBrandResponse> {
  return fetchApi<PublicTenantBrandResponse>(
    `/api/public/tenant-brand/${encodeURIComponent(slug)}`,
    {},
    false,
  );
}
```

- [ ] **Step C2.4 : Commit**

```bash
git add apps/mobile/lib/api/
git commit -m "feat(mobile/api): API client (fetchApi, login, refresh, logout, getTenantBrand)"
```

---

### Task C3 — Zustand stores mobile

**Files:**
- Create: `apps/mobile/lib/auth/store.ts`
- Create: `apps/mobile/lib/tenant/store.ts`

- [ ] **Step C3.1 : Créer `apps/mobile/lib/auth/store.ts`**

```typescript
import { create } from 'zustand';
import type { AuthUser, AuthTenant } from '@ecole-saas/shared';

interface AuthState {
  accessToken: string | null;
  user: AuthUser | null;
  tenant: AuthTenant | null;
  isHydrated: boolean;
  setSession: (session: { accessToken: string; user: AuthUser; tenant: AuthTenant }) => void;
  clear: () => void;
  setHydrated: (v: boolean) => void;
}

/**
 * Auth store mobile — access token en mémoire uniquement.
 * Le refresh token est dans SecureStore (expo-secure-store).
 */
export const useAuthStore = create<AuthState>((set) => ({
  accessToken: null,
  user: null,
  tenant: null,
  isHydrated: false,
  setSession: ({ accessToken, user, tenant }) =>
    set({ accessToken, user, tenant, isHydrated: true }),
  clear: () =>
    set({ accessToken: null, user: null, tenant: null, isHydrated: true }),
  setHydrated: (v) => set({ isHydrated: v }),
}));
```

- [ ] **Step C3.2 : Créer `apps/mobile/lib/tenant/store.ts`**

```typescript
import { create } from 'zustand';
import { DEFAULT_BRAND, type TenantBrand } from '@ecole-saas/shared';

interface TenantState {
  slug: string | null;
  name: string | null;
  brand: TenantBrand;
  setTenant: (slug: string, name: string, brand: Partial<TenantBrand>) => void;
  clear: () => void;
}

export const useTenantStore = create<TenantState>((set) => ({
  slug: null,
  name: null,
  brand: DEFAULT_BRAND,
  setTenant: (slug, name, brand) =>
    set({ slug, name, brand: { ...DEFAULT_BRAND, ...brand } }),
  clear: () => set({ slug: null, name: null, brand: DEFAULT_BRAND }),
}));
```

- [ ] **Step C3.3 : Commit**

```bash
git add apps/mobile/lib/auth/store.ts apps/mobile/lib/tenant/store.ts
git commit -m "feat(mobile/store): Zustand auth + tenant stores"
```

---

### Task C4 — Hook `useTenantBrand`

**Files:**
- Create: `apps/mobile/lib/tenant/use-tenant-brand.ts`

- [ ] **Step C4.1 : Créer `apps/mobile/lib/tenant/use-tenant-brand.ts`**

```typescript
import { useQuery } from '@tanstack/react-query';
import { getTenantBrand } from '@/lib/api/tenant';
import { useTenantStore } from './store';
import type { TenantBrand } from '@ecole-saas/shared';
import { DEFAULT_BRAND } from '@ecole-saas/shared';

/**
 * Charge et met en cache le brand du tenant via TanStack Query (5 min TTL).
 * Met à jour le TenantStore au succès.
 */
export function useTenantBrand(slug: string | null): {
  brand: TenantBrand;
  tenantName: string | null;
  isLoading: boolean;
  error: Error | null;
} {
  const setTenant = useTenantStore((s) => s.setTenant);
  const cachedBrand = useTenantStore((s) => s.brand);
  const cachedName = useTenantStore((s) => s.name);

  const { isLoading, error } = useQuery({
    queryKey: ['tenant-brand', slug],
    queryFn: async () => {
      if (!slug) return null;
      const data = await getTenantBrand(slug);
      setTenant(data.slug, data.name, data.brand);
      return data;
    },
    enabled: !!slug,
    staleTime: 5 * 60 * 1000,
    retry: 2,
  });

  return {
    brand: cachedBrand ?? DEFAULT_BRAND,
    tenantName: cachedName,
    isLoading,
    error: error as Error | null,
  };
}
```

- [ ] **Step C4.2 : Commit**

```bash
git add apps/mobile/lib/tenant/use-tenant-brand.ts
git commit -m "feat(mobile/tenant): useTenantBrand hook (TanStack Query, 5min cache)"
```

---

### Task C5 — i18n FR

**Files:**
- Create: `apps/mobile/lib/i18n/index.ts`
- Create: `apps/mobile/lib/i18n/locales/fr.json`

- [ ] **Step C5.1 : Créer `apps/mobile/lib/i18n/locales/fr.json`**

```json
{
  "onboarding": {
    "title": "Bienvenue sur Klasso",
    "subtitle": "Saisissez le code de votre établissement",
    "placeholder": "Code école (ex: victor-hugo)",
    "continue": "Continuer",
    "loading": "Vérification...",
    "errorNotFound": "Établissement introuvable. Vérifiez le code.",
    "errorNetwork": "Erreur réseau. Réessayez."
  },
  "auth": {
    "title": "Connexion",
    "email": "Adresse email",
    "password": "Mot de passe",
    "submit": "Se connecter",
    "loading": "Connexion...",
    "errorInvalid": "Email ou mot de passe incorrect.",
    "changeSchool": "Changer d'école",
    "errorNetwork": "Erreur réseau. Réessayez."
  },
  "dashboard": {
    "greeting": "Bonjour, {{name}}",
    "role": "Rôle : {{role}}",
    "persona": "Persona : {{persona}}",
    "logout": "Se déconnecter"
  },
  "common": {
    "loading": "Chargement...",
    "error": "Une erreur est survenue.",
    "retry": "Réessayer"
  }
}
```

- [ ] **Step C5.2 : Créer `apps/mobile/lib/i18n/index.ts`**

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import * as Localization from 'expo-localization';
import fr from './locales/fr.json';

i18n.use(initReactI18next).init({
  compatibilityJSON: 'v3',
  lng: Localization.getLocales()?.[0]?.languageCode ?? 'fr',
  fallbackLng: 'fr',
  resources: {
    fr: { translation: fr },
  },
  interpolation: {
    escapeValue: false,
  },
});

export default i18n;
```

- [ ] **Step C5.3 : Mettre à jour `apps/mobile/app/_layout.tsx`** pour importer i18n

Ajouter l'import i18n en haut du fichier :

```tsx
import '../global.css';
import '../lib/i18n'; // initialise i18next au démarrage
import { Stack } from 'expo-router';
import { StatusBar } from 'expo-status-bar';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: { retry: 2, staleTime: 5 * 60 * 1000 },
  },
});

export default function RootLayout() {
  return (
    <QueryClientProvider client={queryClient}>
      <Stack screenOptions={{ headerShown: false }} />
      <StatusBar style="auto" />
    </QueryClientProvider>
  );
}
```

- [ ] **Step C5.4 : Commit**

```bash
git add apps/mobile/lib/i18n/ apps/mobile/app/_layout.tsx
git commit -m "feat(mobile/i18n): i18next FR + expo-localization init"
```

---

### Task C6 — Boot router intelligent + type-check Phase C

**Files:**
- Modify: `apps/mobile/app/index.tsx`

- [ ] **Step C6.1 : Mettre à jour `apps/mobile/app/index.tsx`**

```tsx
import { useEffect } from 'react';
import { View, ActivityIndicator } from 'react-native';
import { Redirect, useRouter } from 'expo-router';
import { useAuthStore } from '@/lib/auth/store';
import { getSavedTenantSlug } from '@/lib/auth/secure-storage';
import { refreshSession } from '@/lib/api/auth';
import { useTenantStore } from '@/lib/tenant/store';

export default function Index() {
  const router = useRouter();
  const isHydrated = useAuthStore((s) => s.isHydrated);
  const accessToken = useAuthStore((s) => s.accessToken);
  const setSession = useAuthStore((s) => s.setSession);
  const setHydrated = useAuthStore((s) => s.setHydrated);
  const tenantSlug = useTenantStore((s) => s.slug);

  useEffect(() => {
    async function boot() {
      // 1. Récupérer le slug sauvegardé
      const savedSlug = await getSavedTenantSlug();

      if (!savedSlug) {
        setHydrated(true);
        router.replace('/(onboarding)/school-code');
        return;
      }

      // 2. Tenter un refresh silencieux
      const session = await refreshSession();
      setHydrated(true);

      if (!session) {
        router.replace('/(auth)/login');
        return;
      }

      setSession({
        accessToken: session.accessToken,
        user: session.user,
        tenant: session.tenant,
      });
      router.replace('/(app)/dashboard');
    }

    boot();
  }, []);

  // Spinner pendant le boot
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <ActivityIndicator size="large" color="#6366f1" />
    </View>
  );
}
```

- [ ] **Step C6.2 : Type-check Phase C**

```bash
pnpm --filter=@klasso/mobile type-check
```

Attendu : 0 errors

- [ ] **Step C6.3 : Commit**

```bash
git add apps/mobile/app/index.tsx
git commit -m "feat(mobile/boot): smart boot router (slug check → refresh → redirect)"
```

- [ ] **Step C6.4 : Ouvrir PR Phase C + CI + merger**

```bash
gh pr create --title "feat(v1.7-A Phase C): theme + brand runtime + i18n + boot router"
gh pr checks --watch && gh pr merge --merge
```

---

## Phase D — Écran (onboarding)/school-code (≈2h)

### Task D1 — Écran school-code

**Files:**
- Create: `apps/mobile/app/(onboarding)/school-code.tsx`

- [ ] **Step D1.1 : Créer `apps/mobile/app/(onboarding)/school-code.tsx`**

```tsx
import { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  ActivityIndicator,
  KeyboardAvoidingView,
  Platform,
  Alert,
} from 'react-native';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { getTenantBrand } from '@/lib/api/tenant';
import { saveTenantSlug } from '@/lib/auth/secure-storage';
import { useTenantStore } from '@/lib/tenant/store';
import { DEFAULT_BRAND } from '@ecole-saas/shared';

export default function SchoolCodeScreen() {
  const { t } = useTranslation();
  const router = useRouter();
  const setTenant = useTenantStore((s) => s.setTenant);

  const [code, setCode] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  async function handleContinue() {
    const slug = code.trim().toLowerCase();
    if (!slug) return;

    setIsLoading(true);
    try {
      const data = await getTenantBrand(slug);
      // Persister le slug pour les prochains démarrages
      await saveTenantSlug(slug);
      // Mettre à jour le store
      setTenant(data.slug, data.name, data.brand);
      // Naviguer vers le login
      router.replace('/(auth)/login');
    } catch (err: unknown) {
      const status = (err as { status?: number })?.status;
      if (status === 404) {
        Alert.alert('Introuvable', t('onboarding.errorNotFound'));
      } else {
        Alert.alert('Erreur', t('onboarding.errorNetwork'));
      }
    } finally {
      setIsLoading(false);
    }
  }

  return (
    <KeyboardAvoidingView
      className="flex-1 bg-white"
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
    >
      <View className="flex-1 justify-center px-8">
        {/* Logo / Titre */}
        <Text className="mb-2 text-center text-3xl font-bold text-indigo-600">
          Klasso
        </Text>
        <Text className="mb-10 text-center text-base text-gray-500">
          {t('onboarding.subtitle')}
        </Text>

        {/* Input code école */}
        <TextInput
          className="mb-4 rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 text-base text-gray-900"
          placeholder={t('onboarding.placeholder')}
          placeholderTextColor="#9ca3af"
          value={code}
          onChangeText={setCode}
          autoCapitalize="none"
          autoCorrect={false}
          returnKeyType="done"
          onSubmitEditing={handleContinue}
          editable={!isLoading}
        />

        {/* Bouton */}
        <TouchableOpacity
          className="items-center rounded-xl bg-indigo-600 py-4"
          onPress={handleContinue}
          disabled={isLoading || !code.trim()}
          style={{ opacity: isLoading || !code.trim() ? 0.6 : 1 }}
        >
          {isLoading ? (
            <ActivityIndicator color="white" />
          ) : (
            <Text className="text-base font-semibold text-white">
              {t('onboarding.continue')}
            </Text>
          )}
        </TouchableOpacity>
      </View>
    </KeyboardAvoidingView>
  );
}
```

- [ ] **Step D1.2 : Type-check**

```bash
pnpm --filter=@klasso/mobile type-check
```

Attendu : 0 errors

- [ ] **Step D1.3 : Commit**

```bash
git add apps/mobile/app/(onboarding)/school-code.tsx
git commit -m "feat(mobile/onboarding): school-code screen (slug lookup + SecureStore persist)"
```

---

### Task D2 — Visual smoke test Phase D

> **🔍 VISUAL SMOKE TEST — Ce que tu dois voir sur ton téléphone**

- [ ] **Step D2.1 : Lancer le dev server**

```bash
# Dans un terminal dédié :
pnpm --filter=@klasso/mobile start
```

Scanner le QR code avec Expo Go.

- [ ] **Step D2.2 : Vérification visuelle**

Tu dois voir sur ton téléphone :

```
┌────────────────────────────────┐
│                                │
│                                │
│         Klasso                 │  ← texte violet/indigo
│                                │
│  Saisissez le code de votre    │
│  établissement                 │
│                                │
│ ┌──────────────────────────┐   │
│ │ Code école (ex: victor.. │   │  ← input gris clair
│ └──────────────────────────┘   │
│                                │
│ ┌──────────────────────────┐   │
│ │      Continuer           │   │  ← bouton indigo, opaque si champ vide → ok
│ └──────────────────────────┘   │  ← bouton semi-transparent si champ vide
│                                │
└────────────────────────────────┘
```

- [ ] **Step D2.3 : Test fonctionnel** (API doit tourner)

Avec `pnpm --filter=@ecole-saas/api dev` lancé :
1. Taper `demo` dans le champ (ou le slug d'un tenant de seed)
2. Appuyer sur "Continuer"
3. Résultat attendu : redirection vers l'écran login (Phase E)

Si l'API n'est pas lancée : le message d'erreur réseau s'affiche — c'est le comportement attendu.

---

## Phase E — Écran (auth)/login (≈2h)

### Task E1 — Écran login

**Files:**
- Create: `apps/mobile/app/(auth)/login.tsx`

- [ ] **Step E1.1 : Créer `apps/mobile/app/(auth)/login.tsx`**

```tsx
import { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  ActivityIndicator,
  KeyboardAvoidingView,
  Platform,
  Alert,
  Image,
} from 'react-native';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { login } from '@/lib/api/auth';
import { useAuthStore } from '@/lib/auth/store';
import { useTenantStore } from '@/lib/tenant/store';
import { DEFAULT_BRAND } from '@ecole-saas/shared';

export default function LoginScreen() {
  const { t } = useTranslation();
  const router = useRouter();
  const setSession = useAuthStore((s) => s.setSession);
  const tenantSlug = useTenantStore((s) => s.slug);
  const tenantName = useTenantStore((s) => s.name);
  const brand = useTenantStore((s) => s.brand);

  const primaryColor = brand?.primaryColor ?? DEFAULT_BRAND.primaryColor;
  const logoUrl = brand?.logoUrl;

  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  async function handleLogin() {
    if (!email.trim() || !password) return;

    setIsLoading(true);
    try {
      const session = await login({
        email: email.trim(),
        password,
        tenantSlug: tenantSlug ?? undefined,
      });
      setSession({
        accessToken: session.accessToken,
        user: session.user,
        tenant: session.tenant,
      });
      router.replace('/(app)/dashboard');
    } catch (err: unknown) {
      const status = (err as { status?: number })?.status;
      if (status === 401) {
        Alert.alert('Erreur', t('auth.errorInvalid'));
      } else {
        Alert.alert('Erreur', t('auth.errorNetwork'));
      }
    } finally {
      setIsLoading(false);
    }
  }

  function handleChangeSchool() {
    router.replace('/(onboarding)/school-code');
  }

  return (
    <KeyboardAvoidingView
      className="flex-1 bg-white"
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
    >
      <View className="flex-1 justify-center px-8">
        {/* Logo ou nom de l'école */}
        {logoUrl ? (
          <Image
            source={{ uri: logoUrl }}
            style={{ width: 72, height: 72, alignSelf: 'center', marginBottom: 8, borderRadius: 12 }}
            resizeMode="contain"
          />
        ) : null}
        <Text
          className="mb-1 text-center text-2xl font-bold"
          style={{ color: primaryColor }}
        >
          {tenantName ?? 'Klasso'}
        </Text>
        <Text className="mb-10 text-center text-base text-gray-500">
          {t('auth.title')}
        </Text>

        {/* Email */}
        <TextInput
          className="mb-3 rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 text-base text-gray-900"
          placeholder={t('auth.email')}
          placeholderTextColor="#9ca3af"
          value={email}
          onChangeText={setEmail}
          autoCapitalize="none"
          keyboardType="email-address"
          returnKeyType="next"
          editable={!isLoading}
        />

        {/* Password */}
        <TextInput
          className="mb-4 rounded-xl border border-gray-200 bg-gray-50 px-4 py-3 text-base text-gray-900"
          placeholder={t('auth.password')}
          placeholderTextColor="#9ca3af"
          value={password}
          onChangeText={setPassword}
          secureTextEntry
          returnKeyType="done"
          onSubmitEditing={handleLogin}
          editable={!isLoading}
        />

        {/* Bouton Se connecter (couleur brand) */}
        <TouchableOpacity
          className="mb-4 items-center rounded-xl py-4"
          style={{
            backgroundColor: primaryColor,
            opacity: isLoading || !email.trim() || !password ? 0.6 : 1,
          }}
          onPress={handleLogin}
          disabled={isLoading || !email.trim() || !password}
        >
          {isLoading ? (
            <ActivityIndicator color="white" />
          ) : (
            <Text className="text-base font-semibold text-white">
              {t('auth.submit')}
            </Text>
          )}
        </TouchableOpacity>

        {/* Changer d'école */}
        <TouchableOpacity onPress={handleChangeSchool} disabled={isLoading}>
          <Text className="text-center text-sm text-gray-400">
            {t('auth.changeSchool')}
          </Text>
        </TouchableOpacity>
      </View>
    </KeyboardAvoidingView>
  );
}
```

- [ ] **Step E1.2 : Type-check**

```bash
pnpm --filter=@klasso/mobile type-check
```

Attendu : 0 errors

- [ ] **Step E1.3 : Commit**

```bash
git add apps/mobile/app/(auth)/login.tsx
git commit -m "feat(mobile/auth): login screen with brand-colored button + change school"
```

---

### Task E2 — Visual smoke test Phase E

> **🔍 VISUAL SMOKE TEST — Ce que tu dois voir sur ton téléphone**

- [ ] **Step E2.1 : Vérification après navigation depuis school-code**

Depuis l'écran school-code, entre un slug valide → tu dois arriver sur :

```
┌────────────────────────────────┐
│                                │
│   [LOGO ici si brand.logoUrl]  │
│                                │
│   Collège Victor Hugo          │  ← couleur brand.primaryColor
│   Connexion                    │  ← gris
│                                │
│ ┌──────────────────────────┐   │
│ │ Adresse email            │   │
│ └──────────────────────────┘   │
│ ┌──────────────────────────┐   │
│ │ Mot de passe             │   │
│ └──────────────────────────┘   │
│                                │
│ ┌──────────────────────────┐   │
│ │    Se connecter          │   │  ← fond couleur brand (indigo par défaut)
│ └──────────────────────────┘   │
│                                │
│       Changer d'école          │  ← gris clair, cliquable
│                                │
└────────────────────────────────┘
```

- [ ] **Step E2.2 : Test fonctionnel**

Avec l'API lancée et le slug `demo` (ou tenant de seed) :
1. Email : `admin@demo.com` (selon seed)
2. Password : `password123` (selon seed)
3. → Redirection vers dashboard (Phase F)

- [ ] **Step E2.3 : Ouvrir PR Phases D+E + CI + merger**

```bash
gh pr create --title "feat(v1.7-A Phases D+E): onboarding school-code + auth login screens"
gh pr checks --watch && gh pr merge --merge
```

---

## Phase F — Dashboard placeholder + nav + logout (≈1h)

### Task F1 — Écran dashboard

**Files:**
- Create: `apps/mobile/app/(app)/dashboard.tsx`

- [ ] **Step F1.1 : Créer `apps/mobile/app/(app)/dashboard.tsx`**

```tsx
import { View, Text, TouchableOpacity, ScrollView } from 'react-native';
import { useRouter } from 'expo-router';
import { useTranslation } from 'react-i18next';
import { useAuthStore } from '@/lib/auth/store';
import { useTenantStore } from '@/lib/tenant/store';
import { logout } from '@/lib/api/auth';
import { DEFAULT_BRAND } from '@ecole-saas/shared';

const PERSONA = process.env.EXPO_PUBLIC_PERSONA ?? 'dev';

export default function DashboardScreen() {
  const { t } = useTranslation();
  const router = useRouter();
  const user = useAuthStore((s) => s.user);
  const clear = useAuthStore((s) => s.clear);
  const tenantName = useTenantStore((s) => s.name);
  const brand = useTenantStore((s) => s.brand);
  const clearTenant = useTenantStore((s) => s.clear);

  const primaryColor = brand?.primaryColor ?? DEFAULT_BRAND.primaryColor;

  async function handleLogout() {
    await logout();
    clear();
    clearTenant();
    router.replace('/(onboarding)/school-code');
  }

  return (
    <ScrollView className="flex-1 bg-white">
      {/* Header brand */}
      <View
        className="px-6 pb-6 pt-14"
        style={{ backgroundColor: primaryColor }}
      >
        <Text className="text-lg font-bold text-white">
          {tenantName ?? 'Klasso'}
        </Text>
      </View>

      <View className="px-6 py-8">
        {/* Greeting */}
        <Text className="mb-2 text-2xl font-bold text-gray-900">
          {t('dashboard.greeting', {
            name: user ? `${user.firstName} ${user.lastName}` : '...',
          })}
        </Text>

        <Text className="mb-1 text-base text-gray-600">
          {t('dashboard.role', { role: user?.role ?? '...' })}
        </Text>

        <Text className="mb-8 text-sm text-gray-400">
          {t('dashboard.persona', { persona: PERSONA })}
        </Text>

        {/* Placeholder modules */}
        <View className="mb-6 rounded-2xl bg-gray-50 p-4">
          <Text className="text-center text-gray-400">
            📚 Modules V1.8+ (Élèves, Classes, Notes...)
          </Text>
        </View>

        {/* Logout */}
        <TouchableOpacity
          className="items-center rounded-xl border border-gray-200 py-4"
          onPress={handleLogout}
        >
          <Text className="text-base text-gray-600">
            {t('dashboard.logout')}
          </Text>
        </TouchableOpacity>
      </View>
    </ScrollView>
  );
}
```

- [ ] **Step F1.2 : Type-check complet**

```bash
pnpm --filter=@klasso/mobile type-check
```

Attendu : 0 errors

- [ ] **Step F1.3 : Commit**

```bash
git add apps/mobile/app/(app)/dashboard.tsx
git commit -m "feat(mobile/app): dashboard placeholder with brand header + persona display + logout"
```

---

### Task F2 — Visual smoke test Phase F + Ouvrir PR

> **🔍 VISUAL SMOKE TEST — Ce que tu dois voir après login réussi**

```
┌────────────────────────────────┐
│■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■│  ← bande couleur brand (header)
│  Collège Victor Hugo           │  ← blanc sur brand
│■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■│
│                                │
│  Bonjour, Jean Dupont          │  ← gros texte
│  Rôle : SCHOOL_ADMIN           │
│  Persona : dev                 │  ← gris clair
│                                │
│ ┌──────────────────────────┐   │
│ │ 📚 Modules V1.8+ (...)   │   │  ← fond gris clair, placeholder
│ └──────────────────────────┘   │
│                                │
│ ┌──────────────────────────┐   │
│ │    Se déconnecter        │   │  ← bouton bordure grise
│ └──────────────────────────┘   │
│                                │
└────────────────────────────────┘
```

**Test du logout :**
- Appuyer "Se déconnecter"
- → retour sur school-code (écran onboarding)
- → le slug est effacé de SecureStore
- → prochain lancement : school-code à nouveau (pas de refresh silencieux)

- [ ] **Step F2.1 : Ouvrir PR Phase F + CI + merger**

```bash
gh pr create --title "feat(v1.7-A Phase F): dashboard placeholder + logout"
gh pr checks --watch && gh pr merge --merge
```

---

## Phase G — ADR + README mobile + Runbook V1.7-B + finalization (≈1h)

### Task G1 — ADR 0004 mobile scaffold

**Files:**
- Create: `docs/adr/0004-mobile-expo-scaffold.md`

- [ ] **Step G1.1 : Créer `docs/adr/0004-mobile-expo-scaffold.md`**

```markdown
# ADR 0004 — Mobile Scaffold Expo SDK 51

**Date :** 2026-05-24
**Statut :** Accepté
**Décideurs :** Équipe Klasso
**Référence spec :** docs/superpowers/specs/2026-05-24-v1.7-mobile-scaffold-design.md

## Contexte

Klasso doit livrer 3 applications mobiles natives (Parent / Enseignant / Direction)
pour iOS et Android. L'équipe est petite, le time-to-market critique.

## Décision

**Expo SDK 51** avec React Native 0.74.5 et Expo Router (file-based routing).

1 codebase — flag `EXPO_PUBLIC_PERSONA` (parent|teacher|admin|dev) → 3 builds EAS en V12.

## Alternatives rejetées

| Option | Raison du rejet |
|---|---|
| Flutter | Écosystème séparé de notre stack TS/React |
| React Native CLI pur | Overhead build infra (Xcode/Gradle) trop lourd pour V1.7 |
| React Native Web only | Pas de stores natifs (App Store / Play Store) |
| Capacitor/Ionic | Performance native inférieure, expertise équipe sur RN |

## Stack retenue

- **Expo SDK 51** — managed workflow (pas de `expo eject`)
- **Expo Router ~3.5** — file-based routing (pattern Next.js App Router)
- **NativeWind v4** — Tailwind CSS en React Native
- **Zustand ^5** — auth store en mémoire (pattern identique au web)
- **expo-secure-store** — refresh token (httpOnly équivalent natif)
- **TanStack Query ^5** — cache brand 5min
- **i18next + expo-localization** — i18n FR (multi-langue V2+)

## Conséquences

- Metro config monorepo obligatoire (watchFolders + nodeModulesPaths)
- `@ecole-saas/shared` transpilé par Metro (pas de build préalable nécessaire)
- EAS Build pour les releases (V12) — pas de local build
- Un seul codebase = tests Maestro communs aux 3 personas
```

- [ ] **Step G1.2 : Commit**

```bash
git add docs/adr/0004-mobile-expo-scaffold.md
git commit -m "docs(adr): 0004 mobile Expo SDK 51 scaffold decision"
```

---

### Task G2 — README mobile (quick start débutant)

**Files:**
- Create: `apps/mobile/README.md`

- [ ] **Step G2.1 : Créer `apps/mobile/README.md`**

```markdown
# @klasso/mobile — Application mobile Klasso

> Application mobile Expo SDK 51 pour les 3 personas Klasso : Parent, Enseignant, Direction.

## ⚡ Quick start (5 minutes)

### Prérequis

- Node.js ≥ 20 ([télécharger](https://nodejs.org))
- pnpm 9+ (`npm install -g pnpm`)
- **Expo Go** sur ton téléphone ([iOS App Store](https://apps.apple.com/app/expo-go/id982107779) | [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent))
- Téléphone et PC **sur le même réseau Wi-Fi**

### 1. Variables d'environnement

```bash
cp apps/mobile/.env.example apps/mobile/.env
# Éditer .env si nécessaire (EXPO_PUBLIC_API_URL)
```

### 2. Installer les dépendances

```bash
pnpm install
```

### 3. Lancer le serveur de développement

```bash
pnpm --filter=@klasso/mobile start
```

### 4. Ouvrir sur ton téléphone

- **iPhone** : ouvre l'app **Appareil photo** → pointe vers le QR code → tape la notification
- **Android** : ouvre **Expo Go** → "Scan QR code" → scanne

L'application se recharge automatiquement à chaque sauvegarde de fichier.

---

## 🔍 Tester les écrans

Le boot router (`app/index.tsx`) redirige automatiquement selon l'état :

| État | Redirection |
|---|---|
| Pas de slug sauvegardé | `(onboarding)/school-code` |
| Slug présent, pas de session | `(auth)/login` |
| Session valide | `(app)/dashboard` |

**Forcer un écran spécifique** : modifier `app/index.tsx` > `<Redirect href="...">` temporairement.

---

## 🎭 Personas

Le flag `EXPO_PUBLIC_PERSONA` sélectionne l'UX cible :

| Valeur | Description |
|---|---|
| `dev` | Affiche tout (persona de dev local) |
| `parent` | UX parent |
| `teacher` | UX enseignant |
| `admin` | UX direction |

En V1.7-A : utilise `dev` pour voir tous les écrans.

---

## 📱 Menu développeur (Debug)

Secouer le téléphone physiquement pour ouvrir le menu :
- **Reload** : recharge l'app
- **Open JS Debugger** : debug Chrome DevTools
- **Toggle Performance Monitor** : FPS, JS heap

---

## 🏗️ Architecture

```
apps/mobile/
  app/                     ← Expo Router (file-based)
    _layout.tsx            ← Root : QueryClient + i18n
    index.tsx              ← Boot router (auto-redirect)
    (onboarding)/          ← Avant connexion
      school-code.tsx      ← Saisie code établissement
    (auth)/                ← Login
      login.tsx
    (app)/                 ← App connectée
      dashboard.tsx
  lib/
    api/                   ← Fetch client + auth + tenant
    auth/                  ← Zustand store + SecureStore
    tenant/                ← Zustand store + useTenantBrand
    i18n/                  ← i18next + locales/fr.json
```

---

## 🛠️ Commandes utiles

```bash
pnpm --filter=@klasso/mobile start          # Dev server (Expo Go)
pnpm --filter=@klasso/mobile ios            # Simulateur iOS (Mac + Xcode requis)
pnpm --filter=@klasso/mobile android        # Émulateur Android (Android Studio requis)
pnpm --filter=@klasso/mobile type-check     # TypeScript strict
pnpm --filter=@klasso/mobile lint           # ESLint
```

---

## ❓ Troubleshooting

| Problème | Solution |
|---|---|
| "Network request failed" | Vérifie que l'API tourne (`pnpm --filter=api dev`) et que `EXPO_PUBLIC_API_URL` pointe bien sur ton IP locale (pas `localhost` depuis le téléphone) |
| QR code ne fonctionne pas | Téléphone et PC sur le même Wi-Fi ? Essaie `exp://TON_IP:8081` manuellement dans Expo Go |
| Type errors après `pnpm install` | `pnpm --filter=@ecole-saas/shared build` puis relancer |
| NativeWind classes sans effet | Redémarre le Metro bundler (`Ctrl+C` puis `pnpm start`) |
| "Unable to resolve module @ecole-saas/shared" | `pnpm install` depuis la racine du monorepo |
```

- [ ] **Step G2.2 : Commit**

```bash
git add apps/mobile/README.md
git commit -m "docs(mobile): README quick start 5min pour débutant Expo"
```

---

### Task G3 — Runbook V1.7-B (activation DNS+Vercel post-livraison klasso.tn)

**Files:**
- Create: `docs/superpowers/runbooks/v1.7-b-dns-activation.md`

- [ ] **Step G3.1 : Créer le dossier et le runbook**

```bash
mkdir -p docs/superpowers/runbooks
```

Créer `docs/superpowers/runbooks/v1.7-b-dns-activation.md` :

```markdown
# Runbook V1.7-B — Activation DNS klasso.tn + Vercel (5 min ops)

> **Déclencher ce runbook dès réception email "Domaine livré" d'OVH.**
> Estimation : 5 minutes d'ops + propagation DNS 5-30 min.

## Prérequis

- [ ] Email OVH "Domaine klasso.tn activé" reçu
- [ ] Accès dashboard OVH (anycast DNS ou zone DNS)
- [ ] Accès Vercel dashboard (projet `jardin`, team `ultra3omda-6664s-projects`)
- [ ] `CLAUDE.md` section V1.7-B relue

---

## Étape 1 — Ajouter les enregistrements DNS OVH

Dans OVH Manager > Domaines > klasso.tn > Zone DNS :

### Enregistrement A (apex)
```
Type : A
Sous-domaine : (vide = @)
Cible : 76.76.21.21
TTL : 3600
```

### Enregistrement CNAME wildcard (subdomains)
```
Type : CNAME
Sous-domaine : *
Cible : cname.vercel-dns.com.
TTL : 3600
```

> Note .tn : OVH/ATI peut mettre 24-72h pour propager les wildcard CNAME
> sur les TLD .tn. L'enregistrement A apex est généralement instantané.

---

## Étape 2 — Ajouter le domaine dans Vercel

```bash
# Via Vercel MCP ou dashboard :
vercel domains add klasso.tn --project jardin
vercel domains add "*.klasso.tn" --project jardin
```

Ou depuis Vercel Dashboard > Projet jardin > Settings > Domains :
- Ajouter `klasso.tn`
- Ajouter `*.klasso.tn`

Vercel va générer un certificat SSL wildcard automatiquement (Let's Encrypt).

---

## Étape 3 — Activer le host-resolver dans Vercel env

Dans Vercel Dashboard > Projet jardin > Settings > Environment Variables :

```
ENABLE_SUBDOMAIN_RESOLVER = true
NEXT_PUBLIC_BASE_DOMAIN = klasso.tn
```

Scope : Production + Preview (pas Local)

→ Déclencher un redeploy :

```bash
gh workflow run deploy.yml
# ou depuis Vercel dashboard > Deployments > Redeploy
```

---

## Étape 4 — Vérification DNS

Depuis ton terminal (une fois le TTL écoulé) :

```bash
# Test apex
curl -I https://klasso.tn

# Test wildcard (avec un tenant de demo)
curl -I https://demo.klasso.tn

# Attendu : HTTP 200 ou redirect 307 vers /t/demo/*
```

---

## Étape 5 — Sanity check fonctionnel

1. Ouvrir `https://demo.klasso.tn` dans le navigateur
2. Vérifier que la page `/t/demo/dashboard` charge correctement
3. Vérifier que `https://klasso.tn` charge la home marketing (ou `/login`)
4. Vérifier que `https://www.klasso.tn` redirige correctement (www = RESERVED_SLUG → pas de rewrite)

---

## Rollback si problème

```bash
# Désactiver le host-resolver sans revert du code
# Vercel Dashboard > env vars > ENABLE_SUBDOMAIN_RESOLVER → false → Redeploy
```

Le code reste dormant, zéro impact.

---

## Post-activation

- [ ] Mettre à jour `docs/roadmap.md` : V1.7-B livré ✅
- [ ] Envoyer notification à l'équipe
- [ ] Monitorer Sentry 30 min après activation
```

- [ ] **Step G3.2 : Commit**

```bash
git add docs/superpowers/runbooks/v1.7-b-dns-activation.md
git commit -m "docs(runbook): V1.7-B DNS + Vercel activation 5 min ops (klasso.tn OVH)"
```

---

### Task G4 — Smoke tests (type-check complet + lint)

- [ ] **Step G4.1 : Type-check tout**

```bash
pnpm type-check
```

Attendu : 0 errors sur web, mobile, shared

- [ ] **Step G4.2 : Lint tout**

```bash
pnpm lint
```

Attendu : 0 warnings non justifiés

- [ ] **Step G4.3 : Build web**

```bash
pnpm build --filter=@ecole-saas/web
```

Attendu : ✓ build réussi, pas de warning TypeScript

- [ ] **Step G4.4 : Tests unitaires web (extractTenantSlugFromHost)**

```bash
pnpm --filter=@ecole-saas/web test -- --run
```

Attendu : 9 passed (extract-tenant-slug.test.ts)

- [ ] **Step G4.5 : Commit smoke test results**

```bash
git add .
git commit -m "chore(ci): confirm all type-check + lint + tests pass for V1.7-A"
```

---

### Task G5 — ⚠️ CP-4 : PR finale V1.7-A + CI + auto-merge

> **CHECKPOINT CP-4 : Avant toute modification `.github/workflows/`**
> En V1.7-A il n'y a PAS de modification des workflows CI existants.
> Ce checkpoint est documenté pour la Phase G5 mais ne bloque pas ici.

- [ ] **Step G5.1 : Ouvrir la PR finale V1.7-A**

```bash
gh pr create \
  --title "feat(v1.7-A): mobile Expo SDK 51 scaffold + middleware host-resolver dormant" \
  --body "$(cat <<'EOF'
## V1.7-A — Klasso Mobile Scaffold

### Changements Phase A
- `packages/typescript-config/react-native.json` — tsconfig Expo SDK 51
- `apps/web/lib/tenant/extract-tenant-slug.ts` — helper + 9 tests
- `apps/web/middleware.ts` — bloc dormant host-resolver (`ENABLE_SUBDOMAIN_RESOLVER` gate)

### Changements Phase B-F
- `apps/mobile/` — scaffold complet Expo SDK 51 :
  - package.json (deps lockées), app.json, tsconfig, metro.config, babel.config, tailwind.config
  - Expo Router : `(onboarding)/school-code`, `(auth)/login`, `(app)/dashboard`
  - Zustand auth + tenant stores (pattern identique web)
  - SecureStore wrapper (refresh token + tenantSlug persisté)
  - TanStack Query + useTenantBrand hook (5min cache)
  - i18next FR (16 clés)
  - Boot router intelligent (slug → refresh → redirect)

### Changements Phase G
- `docs/adr/0004-mobile-expo-scaffold.md`
- `apps/mobile/README.md` — quick start 5 min débutant
- `docs/superpowers/runbooks/v1.7-b-dns-activation.md` — ops 5 min post OVH

### Tests locaux
- 9 unit tests `extractTenantSlugFromHost` : ✅ PASS
- TypeScript strict (web + mobile + shared) : ✅ 0 errors
- Lint : ✅ 0 warnings
- Build web : ✅ OK

### Activation V1.7-B
Runbook : `docs/superpowers/runbooks/v1.7-b-dns-activation.md`
Déclencher dès réception email OVH "klasso.tn livré".

Spec : docs/superpowers/specs/2026-05-24-v1.7-mobile-scaffold-design.md
Roadmap : D21 — docs/roadmap.md
EOF
)"
```

- [ ] **Step G5.2 : CI verte → merge automatique (CLAUDE.md policy)**

```bash
gh pr checks --watch
gh pr merge --merge
```

---

## ✅ Self-Review

### 1. Spec coverage

| Spec § | Exigence | Task |
|---|---|---|
| §2.1 Stack lockée | Expo SDK 51, RN 0.74.5, Expo Router ~3.5, NativeWind v4 | B2 |
| §2.2 Flag PERSONA | `EXPO_PUBLIC_PERSONA` dans app.json + dashboard | B3, F1 |
| §2.3 Brand runtime | `useTenantBrand` + TanStack Query 5min cache | C4 |
| §2.4 SecureStore | Wrapper + STORAGE_KEYS | C1 |
| §3 Middleware dormant | `ENABLE_SUBDOMAIN_RESOLVER` gate | A3 |
| §4.1 school-code screen | Input slug + fetch brand + persist | D1 |
| §4.2 login screen | email/password + brand color + change school | E1 |
| §4.3 dashboard | greeting + role + persona + logout | F1 |
| §5 Metro monorepo | watchFolders + nodeModulesPaths + withNativeWind | B4 |
| §7 extractTenantSlugFromHost | 9 tests, RESERVED_SLUGS | A2 |
| §9 Env vars | EXPO_PUBLIC_API_URL, EXPO_PUBLIC_PERSONA, ENABLE_SUBDOMAIN_RESOLVER | B3, A3 |
| Tutoriel mobile | Expo Go + QR + live-reload + dev menu | Tutoriel section |
| Visual smoke tests | Phases D, E, F | D2, E2, F2 |
| README débutant | Quick start 5 min + troubleshooting | G2 |
| Runbook V1.7-B | DNS OVH + Vercel + ENABLE_SUBDOMAIN_RESOLVER | G3 |
| ADR 0004 | Stack mobile décision documentée | G1 |

### 2. Placeholder scan

- Aucun "TBD", "TODO", "implement later" dans les steps
- Tous les steps de code contiennent le code complet
- Les visual smoke tests ont des ASCII mockups précis

### 3. Type consistency

- `AuthSessionResponse`, `AuthUser`, `AuthTenant`, `TenantBrand`, `DEFAULT_BRAND` — tous importés de `@ecole-saas/shared`
- `useAuthStore` — pattern identique entre web (`apps/web/lib/auth/use-auth-store.ts`) et mobile
- `STORAGE_KEYS` — défini dans `secure-storage.ts` Task C1, utilisé dans `auth.ts` Task C2
- `useTenantStore.clear()` — méthode définie Task C3.2, appelée Task F1
