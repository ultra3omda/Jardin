# TEACHER 3.5 — Dashboard + e2e (clôture Vague 3) Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline).

**Goal:** Clore la Vague 3 TEACHER : confirmer le dashboard enseignant + ajouter une **spec e2e
Playwright teacher** qui valide tous les écrans enseignant refaits (3.1→3.4 + écrans partagés V2).

**Constat dashboard :** le dashboard web (`dashboard/page.tsx`) est **déjà moderne** — `getDashboardConfig(role)`
+ `KpiCard`/`ToDoPanel`/`QuickAction`/`NotesPanel`/`AnnouncementsPanel`. Header de greeting bespoke
(intentionnel, identique aux dashboards admin/parent Wave 1/2.1) → **aucun changement**. Mobile
`dashboard.tsx` : idem (déjà role-based + états). Donc 3.5 = **e2e** (livrable réel) + maj statut spec.

**a11y / RTL :** posés en 3.1→3.4 (labels `sr-only`, `role="alert"`, focus-visible hérités). QA RTL
visuelle = suivi (hors scope).

**Branche / worktree :** `feat/teacher-3.5-dashboard-e2e` (fresh origin/main, install gelée).

---

## Task 1 — e2e `teacher-flows.spec.ts`
Create `apps/web/e2e/teacher-flows.spec.ts` : `loginAsTeacher` (bouton `/Enseignant/i` →
`/dashboard`), test dashboard (bouton « Statistiques » visible, pas de redirection login), puis un
test par écran enseignant (Évaluations, Saisie des notes, Absences & Présences, Devoirs, Mon emploi
du temps, Messages, Annonces) assert le `<h1>` PageHeader visible. Résilient (header rend toujours).
Commit: `test(web): teacher flows e2e smoke (Wave 3 TEACHER)`.

## Task 2 — Statut spec
Marquer `docs/superpowers/specs/2026-06-16-teacher-ux-refonte-design.md` → « complète (3.1→3.5) ».
Commit: `docs: close Wave 3 (TEACHER) spec`.

## Task 3 — Vérif + PR (clôt la Vague 3)
type-check web (+ mobile cohérence) ; push ; PR base main ; CI verte (dont nouvelle spec e2e) → merge ; STOP.

---

## Self-Review
- Spec §4 (3.5) dashboard + e2e ✓. Dashboard déjà moderne (confirmé, pas de churn). Backend : 0.
- e2e : persona « Enseignant » dispo (`prof@demo-ecole.klasso.tn`) ; assertions sur `<h1>` PageHeader
  (rend toujours) + non-redirection login → robuste DB vide. `/classes/[id]/grades` exclu (id dynamique).
- Install gelée. Couverture via e2e (pas de helper pur).
