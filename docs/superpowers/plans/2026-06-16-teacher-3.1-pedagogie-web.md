# TEACHER 3.1 — Pédagogie web (évaluations + saisie notes) Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Steps `- [ ]`.

**Goal:** Refondre les 2 écrans pédagogie web enseignant (`evaluations/page.tsx`,
`classes/[id]/grades/grades-client.tsx`) avec les états Vague 1 — sans backend ni logique de
mutation/validation serveur touchée.

**Scope :** PageHeader + Skeleton/EmptyState/ErrorRetry (erreurs avalées surfacées) ; helper pur
testé `parseScoreInput` (validation note client, utilisé dans la saisie). Modals/forms CRUD,
TanStack queries/mutations, période clôturée (`isClosed`) : **inchangés**.

**Branche / worktree :** `feat/teacher-3.1-pedagogie-web` (fresh origin/main, install gelée).

---

## File Structure
- **Web new :** `apps/web/lib/grades/score-input.ts` (+`__tests__/score-input.test.ts`).
- **Web modifiés :** `evaluations/page.tsx`, `classes/[id]/grades/grades-client.tsx`.

## Task 1 — `parseScoreInput` (pur, testé)
```ts
/** Parse une note saisie : nombre fini dans [0, maxScore], sinon null. Accepte la virgule FR. */
export function parseScoreInput(raw: string, maxScore: number): number | null {
  const trimmed = raw.trim();
  if (trimmed === '') return null;
  const n = Number(trimmed.replace(',', '.'));
  if (!Number.isFinite(n)) return null;
  if (n < 0 || n > maxScore) return null;
  return n;
}
```
Tests : vide→null, '15.5'→15.5, '12,25'→12.25, '25'/ '-1' (maxScore 20)→null, 'abc'→null.
Commit: `feat(web): add parseScoreInput grade helper`.

## Task 2 — `evaluations/page.tsx`
Edits : imports (lucide `ClipboardList`, `Link`, `PageHeader`/`TableSkeleton`/`EmptyState`/`ErrorRetry`) ;
`loading`+`fetchError` → `loadState` union ; `load()` surface l'échec de la liste évals
(`evRes.status==='rejected'` → error) ; header → `PageHeader` (action « + Nouvelle évaluation ») ;
bloc loading/empty → `TableSkeleton`/`ErrorRetry`/`EmptyState` ; `<a href=.../grades>` → `Link`.
Modal CRUD + `confirm()` delete inchangés.
Commit: `feat(web): evaluations refonte (PageHeader/Skeleton/EmptyState/ErrorRetry)`.

## Task 3 — `grades-client.tsx`
Edits : imports (+UI primitives + `parseScoreInput`) ; `<h1>` → `PageHeader` + gate
`primaryLoading` (classQ/subjectsQ/periodsQ/studentsQ) → `TableSkeleton` / `classQ.isError` →
`ErrorRetry(refetch)` ; empty évals → `EmptyState` ; `EvaluationCard` onBlur → `parseScoreInput`
(applique aussi le clamp maxScore). Période select, create form, `isClosed`, mutations : inchangés.
Commit: `feat(web): grade entry refonte (PageHeader/Skeleton/ErrorRetry) + parseScoreInput`.

## Task 4 — Vérif + PR
type-check web (+ mobile par cohérence) ; push ; PR base main ; CI verte → merge ; STOP avant 3.2.

---

## Self-Review
- Spec §4 (3.1) évaluations + saisie notes ✓. Backend : 0 endpoint/migration ; mutations/`isClosed` intacts.
- Sensible (notes) : seul un **parsing client** est ajouté (clamp [0,maxScore]) ; rien côté serveur.
- Types : web primitives vérifiées (V2) ; `parseScoreInput` pur ; TanStack `isLoading`/`isError`/`refetch`.
- Tests : `parseScoreInput` (Vitest). Install gelée.
