# PARENT 2.5 — Communication (messagerie / annonces) Implementation Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Steps use `- [ ]`.

**Goal:** Refondre les écrans communication parent (messagerie 1:1, annonces) web + mobile avec
les états Vague 1 (PageHeader/Skeleton/EmptyState/ErrorState) — sans backend.

**Architecture:** Extraction d'un helper pur testé (`conversation.ts` : `otherParticipant`,
`formatConversationPreview`) ; remplacement des états ad-hoc (« Chargement… », bordures pointillées,
texte d'erreur) par les primitives. Logique messagerie/Socket.IO et CRUD annonces **inchangée**.

**Tech:** Next 14 (client + un server component), Tailwind, lucide-react, Vitest ; Expo/RN,
`@klasso/ui-mobile`.

**Branche / worktree :** `feat/parent-2.5-communication` (fresh origin/main avec 2.4).

**Scope notes :** Notifications **différées** (bell web déjà OK, mobile déjà modernisé, page web a déjà
un skeleton manuel → polish léger reporté à 2.7). Annonces mobile = admin only (hors persona parent).

---

## File Structure
- **Web new :** `apps/web/lib/messaging/conversation.ts` (+`__tests__/conversation.test.ts`).
- **Web modifiés :** `messages/page.tsx`, `messages/messages-list.tsx`,
  `messages/[id]/conversation-thread.tsx`, `announcements/page.tsx`.
- **Mobile modifiés :** `app/(app)/messages/index.tsx`, `app/(app)/messages/[id].tsx`.

---

## Task 1: Web — `conversation` helpers (pur, testés)

Create `apps/web/lib/messaging/conversation.ts`:

```ts
import type { Conversation } from '@/lib/api/messaging';

const PREVIEW_MAX = 80;

export function otherParticipant(c: Conversation, currentUserId: string) {
  return c.participants.find((p) => p.userId !== currentUserId);
}

/** Aperçu une ligne du dernier message ("Vous : …", tronqué à 80). */
export function formatConversationPreview(c: Conversation, currentUserId: string): string {
  if (!c.lastMessage) return 'Conversation ouverte. Aucun message encore.';
  const prefix = c.lastMessage.senderId === currentUserId ? 'Vous : ' : '';
  const body =
    c.lastMessage.body.length > PREVIEW_MAX
      ? `${c.lastMessage.body.slice(0, PREVIEW_MAX - 3)}…`
      : c.lastMessage.body;
  return `${prefix}${body}`;
}
```

Test `__tests__/conversation.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import type { Conversation } from '@/lib/api/messaging';
import { otherParticipant, formatConversationPreview } from '../conversation';

function makeConv(over: Partial<Conversation>): Conversation {
  return {
    id: 'c1',
    participants: [
      { userId: 'me', firstName: 'Moi', lastName: 'X' },
      { userId: 'u2', firstName: 'Sara', lastName: 'Ben' },
    ],
    unreadCount: 0,
    updatedAt: '2026-01-01T00:00:00.000Z',
    lastMessage: null,
    ...over,
  } as unknown as Conversation;
}

describe('otherParticipant', () => {
  it('returns the participant that is not the current user', () => {
    expect(otherParticipant(makeConv({}), 'me')?.userId).toBe('u2');
  });
});

describe('formatConversationPreview', () => {
  it('returns an opener when there is no last message', () => {
    expect(formatConversationPreview(makeConv({}), 'me')).toContain('Aucun message');
  });
  it('prefixes "Vous : " when the last message is mine', () => {
    const c = makeConv({ lastMessage: { id: 'm', senderId: 'me', body: 'Bonjour' } as never });
    expect(formatConversationPreview(c, 'me')).toBe('Vous : Bonjour');
  });
  it('truncates long messages to 77 chars + ellipsis', () => {
    const c = makeConv({ lastMessage: { id: 'm', senderId: 'u2', body: 'x'.repeat(100) } as never });
    const out = formatConversationPreview(c, 'me');
    expect(out.endsWith('…')).toBe(true);
    expect(out.length).toBe(78);
  });
});
```

Commit: `feat(web): extract conversation preview helpers`.

## Task 2: Web — `messages/page.tsx` (PageHeader) + `messages-list.tsx` (Skeleton/Empty/Error + helpers)
Full rewrites (see implementation). `messages-list` adds `isError/refetch`, `ConversationSkeleton`,
`EmptyState` (icon `MessageSquare`), `ErrorRetry`, and imports the Task 1 helpers.
Commit: `feat(web): messages list refonte (PageHeader/Skeleton/EmptyState/ErrorRetry)`.

## Task 3: Web — `conversation-thread.tsx`
Targeted edits: import `Skeleton`; loading text → chat-bubble Skeletons; add `role="alert"` to the
Socket.IO fallback bar. Logique Socket.IO/mutation inchangée.
Commit: `feat(web): conversation thread loading skeleton + alert role`.

## Task 4: Web — `announcements/page.tsx`
Targeted edits: imports (`PageHeader`, `Skeleton`, `EmptyState`, `ErrorRetry`, `Megaphone`) ;
`loading` boolean → `loadState` union (loading/error/ready) ; header → `PageHeader` (bouton admin en
`actions`) ; bloc loading/empty → Skeleton grid / `ErrorRetry` / `EmptyState`. Modals CRUD inchangées.
Commit: `feat(web): announcements refonte (PageHeader/Skeleton/EmptyState/load error)`.

## Task 5: Mobile — `messages/index.tsx` + `messages/[id].tsx`
Edits: import `EmptyState/ErrorState/Skeleton` ; `index` → Skeleton rows + `ErrorState` + `EmptyState`
(`chatbubbles-outline`) ; `[id]` → bubble Skeletons + `ErrorState` (garde `ActivityIndicator` du bouton
d'envoi). Logique inchangée.
Commit: `feat(mobile): messages refonte (Skeleton/ErrorState/EmptyState)`.

## Task 6: Vérif + PR
type-check web + mobile (verts) ; push ; PR base main ; CI verte → merge ; STOP.

---

## Self-Review
- Spec §6 (2.5) messagerie + annonces web/mobile ✓ ; notifications différées (justifié : déjà partiellement modernisé) ; annonces mobile = admin (hors persona).
- Backend : 0 endpoint/migration ; Socket.IO + CRUD intacts.
- Types : `Conversation`/`Participant` de `@/lib/api/messaging` ; web `Skeleton` accepte `className`
  (vérifié 2.4) ; mobile `Skeleton` accepte `width/height/radius/style`, `ErrorState`/`EmptyState`
  exportés (vérifié 2.3/2.4).
- Tests : `conversation.ts` (Vitest). Mobile présentationnel (type-check).
