# TEACHER 3.4 — Devoirs management (mobile) Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline).

**Goal:** Aligner les écrans mobile devoirs enseignant sur les états Vague 1 (Skeleton/ErrorState).

**Scope (réduit après mapping) :** `pedagogy/homework/index.tsx` + `pedagogy/homework/[id].tsx`
(ActivityIndicator + texte d'erreur → Skeleton/ErrorState). `homework/new.tsx` = déjà moderne
(FormField/Picker). Web `homework-client` = déjà fait en 2.7. **Aucun backend** ; suivi des rendus /
mutations inchangés.

**Branche / worktree :** `feat/teacher-3.4-devoirs` (fresh origin/main, install gelée).

---

## Task 1 — `homework/index.tsx`
Import `ErrorState`/`Skeleton`, retirer `ActivityIndicator`. Loading → `Skeleton` ×4 ; error
(Pressable+Text) → `ErrorState`+`refetch`. `EmptyState` partagé déjà OK.
Commit: `feat(mobile): homework list states (Skeleton/ErrorState)`.

## Task 2 — `homework/[id].tsx` (détail + suivi rendus)
Import `ErrorState`/`Skeleton`, retirer `ActivityIndicator`. Early-return loading → `Skeleton` ;
early-return error → `ErrorState`+`refetch`. Boutons de suivi (`upsert`) inchangés.
Commit: `feat(mobile): homework detail states (Skeleton/ErrorState)`.

## Task 3 — Vérif + PR
type-check mobile ; push ; PR base main ; CI verte → merge ; STOP avant 3.5.

---

## Self-Review
- Spec §4 (3.4) devoirs mobile ✓ ; `new` déjà moderne, web fait en 2.7. Backend : 0 ; suivi rendus intact.
- Types : `Skeleton`/`ErrorState` exportés ; `refetch` via useQuery. Install gelée. Présentationnel → type-check.
