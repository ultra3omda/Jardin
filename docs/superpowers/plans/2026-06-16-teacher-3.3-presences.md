# TEACHER 3.3 — Présences (saisie) Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline).

**Goal:** Refondre `StaffAbsencesView` (saisie présences journalières, web) sous les états Vague 1.
**Web only** (pas d'écran présences mobile). **Aucun backend** ; logique de saisie/`bulk` inchangée.

**Contexte :** `absences/page.tsx` contient `ParentAbsencesView` (fait en 2.6) + `StaffAbsencesView`
(laissé legacy). Les primitives `PageHeader`/`TableSkeleton`/`EmptyState`/`ErrorRetry` + le module
`AttendanceStatusBadge` sont **déjà importés** (2.6) → aucun nouvel import.

**Branche / worktree :** `feat/teacher-3.3-presences` (fresh origin/main, install gelée).

---

## Task 1 — `absences/page.tsx` (`StaffAbsencesView`)
Edits ciblés (la grille radio de saisie, `loadStudents`, `handleSave`/`bulk`, counts : inchangés) :
1. `header` maison → `PageHeader` (bouton « Enregistrer » en `actions`).
2. selects → labels `sr-only` (a11y).
3. `fetchError` banner → ajout `role="alert"`.
4. loading « Chargement… » → `TableSkeleton` (gate sur `loadingClasses || loadingStudents`) ;
   empty dashed → `EmptyState`.

> Note : la pastille `AttendanceStatusBadge` sert l'affichage parent (2.6) ; ici la saisie est une
> grille de radios par statut — non remplaçable par la pastille. Les chips de comptage gardent le ton.
Commit: `feat(web): staff attendance entry refonte (PageHeader/Skeleton/EmptyState/a11y)`.

## Task 2 — Vérif + PR
type-check web (+ mobile cohérence) ; push ; PR base main ; CI verte → merge ; STOP avant 3.4.

---

## Self-Review
- Spec §4 (3.3) présences saisie ✓ ; web only (mobile absent). Backend : 0 ; `bulk`/records intacts.
- Types : primitives déjà importées (2.6) ; `AttendanceStatus` partagé. Install gelée.
- Présentationnel + a11y → couverture type-check (pas de helper pur).
