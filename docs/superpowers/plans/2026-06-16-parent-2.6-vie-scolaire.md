# PARENT 2.6 — Vie scolaire (absences / EDT / devoirs) Implementation Plan

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Steps use `- [ ]`.

**Goal:** Refondre les écrans vie-scolaire parent (présences, EDT, devoirs) web + mobile avec les
états Vague 1 (PageHeader/ScreenHeader/Skeleton/EmptyState/ErrorState) — sans backend.

**Architecture:** Nouveau `AttendanceStatusBadge` (+ labels/tons purs, testé) ; remplacement des
états ad-hoc (« Chargement… », bordures pointillées, erreurs **avalées**) par les primitives ;
mobile : `ActivityIndicator`/texte d'erreur → `Skeleton`/`ErrorState`+retry.

**Scope notes :** Cible les **4 écrans à erreur avalée / texte brut** : web absences + web EDT,
mobile EDT + mobile devoirs. **Différé à 2.7** : `homework-client.tsx` web (déjà skeleton+erreur,
manque PageHeader) et `parent/observations.tsx` mobile (déjà ErrorState+EmptyState, manque Skeleton).
Absences = pas d'écran mobile (web only). `StaffAbsencesView` (admin) non touché (réutilise les maps).

**Branche / worktree :** `feat/parent-2.6-vie-scolaire` (fresh origin/main avec 2.5, install gelée).

---

## File Structure
- **Web new :** `apps/web/components/attendance/attendance-status-badge.tsx`
  (+`__tests__/attendance-status-badge.test.tsx`).
- **Web modifiés :** `absences/page.tsx` (ParentAbsencesView), `schedule/page.tsx`.
- **Mobile modifiés :** `app/(app)/parent/schedule.tsx`, `app/(app)/parent/homework.tsx`.

---

## Task 1: Web — `AttendanceStatusBadge` (module + composant + test)

Create `apps/web/components/attendance/attendance-status-badge.tsx`:

```tsx
export type AttendanceStatus = 'PRESENT' | 'ABSENT' | 'LATE' | 'EXCUSED';

export const ATTENDANCE_STATUS_LABELS: Record<AttendanceStatus, string> = {
  PRESENT: 'Présent',
  ABSENT: 'Absent',
  LATE: 'Retard',
  EXCUSED: 'Excusé',
};

export const ATTENDANCE_STATUS_TONE: Record<AttendanceStatus, string> = {
  PRESENT: 'bg-green-100 text-green-800 border-green-300',
  ABSENT: 'bg-red-100 text-red-800 border-red-300',
  LATE: 'bg-yellow-100 text-yellow-800 border-yellow-300',
  EXCUSED: 'bg-slate-100 text-slate-700 border-slate-300',
};

/** Pastille de statut de présence — libellé FR + ton. */
export function AttendanceStatusBadge({ status }: { status: AttendanceStatus }) {
  return (
    <span
      className={`inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium ${ATTENDANCE_STATUS_TONE[status]}`}
    >
      {ATTENDANCE_STATUS_LABELS[status]}
    </span>
  );
}
```

Test `__tests__/attendance-status-badge.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { AttendanceStatusBadge } from '../attendance-status-badge';

describe('AttendanceStatusBadge', () => {
  it('renders the French label', () => {
    render(<AttendanceStatusBadge status="ABSENT" />);
    expect(screen.getByText('Absent')).toBeTruthy();
  });
  it('applies a tone class per status', () => {
    render(<AttendanceStatusBadge status="PRESENT" />);
    expect(screen.getByText('Présent').className).toContain('green');
  });
});
```

Commit: `feat(web): add AttendanceStatusBadge (shared attendance status pill)`.

## Task 2: Web — `absences/page.tsx` (ParentAbsencesView)
Targeted edits: imports (lucide `CalendarCheck`, `PageHeader`/`TableSkeleton`/`EmptyState`/`ErrorRetry`,
+ from the module `AttendanceStatusBadge` + maps aliased `STATUS_LABELS`/`STATUS_COLORS` + type),
remove the local type/maps. ParentAbsencesView → `loadState` (loading/error/ready, error no longer
swallowed), `PageHeader`, `TableSkeleton`, `ErrorRetry`+retry, `EmptyState`, `AttendanceStatusBadge`.
`StaffAbsencesView` unchanged (uses the imported aliases).
Commit: `feat(web): parent attendance refonte (PageHeader/Skeleton/EmptyState/ErrorRetry + badge)`.

## Task 3: Web — `schedule/page.tsx`
Full rewrite: merge the two primary loads into one `loadPrimary` useCallback with `loadState`
(error surfaced + retry), `PageHeader`, `TableSkeleton` while loading, `EmptyState` for no-class /
no-slots, `ErrorRetry` on failure. Grid + per-class detail logic unchanged.
Commit: `feat(web): schedule refonte (PageHeader/Skeleton/EmptyState/ErrorRetry)`.

## Task 4: Mobile — `parent/schedule.tsx` + `parent/homework.tsx`
Edits: import `ErrorState`/`Skeleton`, drop `ActivityIndicator`. Top-level loading → Skeleton group;
`<Text>Erreur…` → `ErrorState`+`refetch`. Keep `EmptyState`. In `ChildSchedule`, per-child
`ActivityIndicator` → small `Skeleton`. Card/grid logic unchanged.
Commit: `feat(mobile): schedule + homework refonte (Skeleton/ErrorState)`.

## Task 5: Vérif + PR
type-check web + mobile (verts) ; push ; PR base main ; CI verte → merge ; STOP.

---

## Self-Review
- Spec §6 (2.6) absences + EDT + devoirs, web/mobile ✓. Partiels (homework-client web, observations mobile) différés à 2.7 (justifié : déjà skeleton/ErrorState). Absences mobile inexistant (web only).
- Backend : 0 endpoint/migration. `StaffAbsencesView` (admin) intact.
- Types : `AttendanceStatus` partagé ; web `Skeleton`/`TableSkeleton`/`PageHeader`/`EmptyState`/`ErrorRetry` (vérifiés) ; mobile `Skeleton`/`ErrorState` exportés (vérifié) ; `useMyChildren`/`useChildrenHomework` exposent `refetch` (useQuery).
- Tests : `AttendanceStatusBadge` (Vitest). Mobile présentationnel (type-check).
- **Install gelée** (`--frozen-lockfile`) pour éviter la dérive `@types/react` (leçon 2.5).
