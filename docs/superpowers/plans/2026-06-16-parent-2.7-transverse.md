# PARENT 2.7 — Passe transverse (reliquats + e2e) — clôture Vague 2

> REQUIRED SUB-SKILL: superpowers:executing-plans (inline). Steps use `- [ ]`.

**Goal:** Clore la Vague 2 PARENT : finir les 2 écrans « partiels » (devoirs web, observations
mobile) et ajouter une **spec e2e Playwright parent** qui valide tous les écrans refaits (2.3→2.6).

**Architecture / scope :**
- **Reliquats** : web `homework/page.tsx` (header → `PageHeader`) + `homework-client.tsx`
  (loading pulse ad-hoc → `Skeleton`, bloc erreur → `ErrorRetry`, empty ad-hoc → `EmptyState`) ;
  mobile `parent/observations.tsx` (loading `ActivityIndicator` → `Skeleton` ; ErrorState/EmptyState
  déjà présents).
- **e2e parent** : nouvelle spec `apps/web/e2e/parent-flows.spec.ts` — demo-login persona **Parent**
  (bouton `/^Parent$/i` → `/dashboard`), puis visite des 7 écrans parent, assert le titre `PageHeader`
  visible + pas de crash (`p:has-text(/^Erreur :/)` absent). Résilient (le `<h1>` rend toujours).
- **a11y / RTL** : hérités des primitives Phase 0 (labels `sr-only` sur selects, `aria-label` sur
  boutons-icônes, focus-visible, `dir` next-intl) déjà posés en 2.3→2.6. QA RTL visuelle approfondie
  **non incluse** (nécessite run navigateur en arabe) — notée comme suivi.

**Branche / worktree :** `feat/parent-2.7-transverse` (fresh origin/main avec 2.6, install gelée).

---

## Task 1: Web — homework (page + client)

**page.tsx** — `header` → `PageHeader` :
```tsx
export const dynamic = 'force-dynamic';

import { PageHeader } from '@/components/ui/page-header';
import { HomeworkClient } from './homework-client';

export default function HomeworkPage() {
  return (
    <div className="space-y-6">
      <PageHeader
        title="Devoirs"
        description="Travail à faire par classe : création, échéances et suivi des rendus."
      />
      <HomeworkClient />
    </div>
  );
}
```
> NB : garder `export const dynamic` AVANT les imports n'est pas valide → on le laisse après les
> imports (déplacer en tête de fonction non requis ; mettre la directive sous les imports).

**homework-client.tsx** — imports + remplacer loading/error/empty par les primitives :
- Ajouter : `import { BookOpen } from 'lucide-react';`, `Skeleton`, `EmptyState`, `ErrorRetry`.
- Loading → `Skeleton` ×3 ; Error → `ErrorRetry` ; Empty → `EmptyState` (action `openCreate`).

Commit: `feat(web): homework refonte — PageHeader + Skeleton/EmptyState/ErrorRetry`.

## Task 2: Mobile — observations
`parent/observations.tsx` : retirer `ActivityIndicator` de l'import RN, ajouter `Skeleton` à
`@klasso/ui-mobile` ; loading → `Skeleton` ×3. ErrorState/EmptyState déjà OK.
Commit: `feat(mobile): observations loading skeleton`.

## Task 3: Web — e2e parent flows
Create `apps/web/e2e/parent-flows.spec.ts` (voir implémentation) : `loginAsParent` + un test par
écran (Paiements, Notes de mes enfants, Bulletins, Messages, Annonces, Présences de mes enfants,
Emploi du temps) assert heading visible + pas de bandeau « Erreur : ».
Commit: `test(web): parent flows e2e smoke (Wave 2 PARENT)`.

## Task 4: Vérif + PR (clôt la Vague 2)
type-check web + mobile ; push ; PR base main ; CI verte (dont la nouvelle spec e2e) → merge ; STOP.

---

## Self-Review
- Spec §6 (2.7) reliquats + e2e parent ✓. a11y/RTL : primitives Phase 0 (posées 2.3→2.6) ; QA RTL visuelle = suivi explicite (honnête, pas sur-promis).
- Backend : 0 endpoint/migration. homework-client : comportement create préservé (action EmptyState = `openCreate`).
- Types : web primitives vérifiées ; mobile `Skeleton` exporté ; `refetch` via useResource/useQuery.
- e2e : demo-login Parent dispo (`parent@demo-ecole.klasso.tn`, seed self-healing) ; pattern calqué sur `roster.spec.ts`/`messaging.spec.ts`. Assertions sur le `<h1>` PageHeader (rend toujours) → robuste même DB vide.
- Install gelée (leçon 2.5).
